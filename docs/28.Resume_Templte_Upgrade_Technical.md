# 技术模板升级

针对 **Technical (技术型)** 模板，这类用户通常有大量的技术栈名词、GitHub 项目链接以及复杂的职责描述。设计师的核心任务是：**在高信息密度下保持极度的条理性，并利用视觉对比突出核心技能。**

以下是我为你产出的 **「Technical 模板设计方案推导图」**（Design Logic Map）：

---

### 🛠️ Technical 模板：视觉设计方案推导图

#### 1. 版式骨架 (Layout Structure)

- **设计推导：** 互联网技术岗简历通常由 HR 和技术面试官共同阅读。HR 看“关键词匹配”，面试官看“项目深度”。
- **落地方案：** \* **非对称单栏流 (Asymmetric Single Column)：** 保持单栏以确保 ATS 100% 兼容，但通过内边距（Padding）将日期、地点等“次要元数据”统一向右侧对齐或通过侧边留白形成视觉上的“时间轴感”。
- **核心技能前置：** 在 `Summary` 或 `Basics` 之后立即放置 `Skills`，并采用“分类标签”布局，而非纯文字排版。

#### 2. 字体系统 (Typography & Hierarchy)

- **设计推导：** 简历中最忌讳“字号乱跳”。技术型简历需要一种“冷静且硬朗”的工业感。
- **落地方案：**
- **三级字号阶梯：**
- **L1 (姓名/模块标题):** 14pt/16px, Semibold, 字间距（Letter Spacing）略微加宽，提升高级感。
- **L2 (公司名/职位/项目名):** 11pt/14px, Bold, 纯黑 (#111827)。
- **L3 (描述正文/日期):** 9.5pt/13px, Regular, 深灰 (#4B5563)，行高 1.6 确保不粘连。

- **Monospace 混合排版：** 对技术栈（如 `React`, `Golang`, `K8s`）建议使用**等宽字体 (Monospace)** 的轻量装饰，模拟代码质感，使其在段落中自动脱颖而出。

#### 3. 装饰元素与垂直节律 (UI Elements & Rhythm)

- **设计推导：** 技术岗不需要花哨的插图，但需要“结构组件”。
- **落地方案：**
- **时间轴线 (Timeline Anchor)：** 在左侧加入 1px 的浅灰色 `#E5E7EB` 垂直虚线，串联工作经历，给读者一种职业发展连贯的心理暗示。
- **链接视觉化：** 所有的 GitHub 或 Demo 链接不再是长长的 URL，而是使用“图标 + 简短文本”并加下划线，降低视觉噪音。
- **技能 Badge：** 拒绝彩虹色。使用极浅灰背景 + 墨灰色文字的胶囊形（Pill）标签，仅通过字体粗细区分掌握程度。

#### 4. 信息对比策略 (Contrast Strategy)

- **设计推导：** 读者的视线应该被引导，而不是在文字海洋里打捞。
- **落地方案：**
- **“黑-灰-淡”三色原则：** 核心名词用黑，辅助信息用灰，分隔线用淡色。
- **Bullet Point 优化：** 弃用默认圆点，改用更具几何感的“中型方块”或“细箭头”，并在每条描述间留出 4px 的额外呼吸感。

---

### 🎨 视觉关键细节示意 (Visual Specs)

| 模块              | 视觉处理重点          | Tailwind 建议类名示例                                                 |
| ----------------- | --------------------- | --------------------------------------------------------------------- |
| **Section Title** | 顶部 2px 粗黑短线装饰 | `border-t-2 border-black pt-2 mb-4 uppercase tracking-wider`          |
| **Job Title**     | 强调职位而非公司      | `text-gray-900 font-bold text-lg`                                     |
| **Tech Stacks**   | 标签化处理，等宽字体  | `font-mono bg-slate-50 px-1.5 py-0.5 rounded border border-slate-200` |
| **Dates**         | 右对齐，斜体或弱色    | `text-gray-500 font-light italic tabular-nums`                        |

---

### 🛠️ Technical 模板：低保真视觉原型示意 (Blueprint)

```text
+-------------------------------------------------------------------------------+
| [ 姓名 / NAME ] (16px, Bold, Tracking-wide)                                    |
| Full Stack Engineer | 5 Years Experience                                      |
| 📧 email@example.com  | 📱 138-0000-0000 | 🔗 github.com/username | 📍 Beijing    |
+-------------------------------------------------------------------------------+
|                                                                               |
| [ 核心技能 / SKILLS ] --------------------------------------------------------- |
| (Section Header: 14px, Semibold, Uppercase, Border-Top: 2px Black)            |
|                                                                               |
|  前端: [ React ] [ Next.js ] [ TypeScript ] [ Tailwind ] [ Three.js ]         |
|  后端: [ Node.js ] [ Go ] [ PostgreSQL ] [ Redis ] [ Docker ]                 |
|  (Badge Style: Mono Font, 12px, Bg-slate-50, Border-slate-200, Rounded-sm)    |
|                                                                               |
+-------------------------------------------------------------------------------+
|                                                                               |
| [ 工作经历 / EXPERIENCE ] ----------------------------------------------------- |
|                                                                               |
|  ●  [ 资深前端开发工程师 ] @ [ 某某科技互联网公司 ]             [ 2021.06 - 至今 ] |
|  |  (Job Title: 14px, Bold)  (Company: 14px)            (Date: 12px, Muted, Italic)|
|  |                                                                            |
|  |  • 负责 AI 简历助手的架构设计与实现，采用 Next.js 14 App Router 提升 SEO      |
|  |  • 优化 PDF 渲染引擎，将 A4 导出成功率从 85% 提升至 99.9%                    |
|  |  • 建立前端原子化组件库，减少 40% 的冗余 CSS 代码                             |
|  |  (Bullet: 13px, Leading-relaxed, Text-gray-600)                             |
|  |                                                                            |
|  ○  [ 前端开发工程师 ] @ [ 某初创软件工作室 ]                  [ 2019.03 - 2021.05 ]|
|     (Timeline Anchor: 1px Vertical Gray Line connecting the dots)             |
|                                                                               |
+-------------------------------------------------------------------------------+
|                                                                               |
| [ 项目项目 / PROJECTS ] ------------------------------------------------------- |
|                                                                               |
|  [ OpenSource-Resume-Builder ] | 🔗 github.com/repo            [ 个人项目 ]     |
|  (Project Name: 14px, Bold)      (Icon + Link: Blue-600)                       |
|                                                                               |
|  • 基于 GPT-4o 的智能简历润色引擎，支持多语言实时切换                           |
|  • 技术栈: `React` `Zustand` `OpenAI API` `Vercel`                            |
|    (Tech Stack in descriptions: Monospace background highlights)              |
|                                                                               |
+-------------------------------------------------------------------------------+
|                                                                               |
| [ 教育背景 / EDUCATION ] ------------------------------------------------------ |
|                                                                               |
|  [ 某某重点大学 ]  -  [ 计算机科学与技术 ]  -  [ 本科 ]           [ 2015 - 2019 ]  |
+-------------------------------------------------------------------------------+

```

---

### 🎨 深度打磨：三个关键“视觉锚点”

为了让这个模板从“能用”变成“精致”，我们需要在代码中落地以下细节：

#### 1. 垂直节律 (Vertical Rhythm)

- **痛点：** 目前很多模板看起来“乱”，是因为模块间距（Margin）是随机的。
- **设计指令：** \* `Section` 之间统一使用 `mb-8` (32px)。
- `Experience Item` 内部，标题与描述之间使用 `mt-2` (8px)。
- 这种以 8px 为基准的等比间距会让简历有一种“数学上的秩序感”。

#### 2. “技术感”的排版 (The Tech Feel)

- **Monospace 的妙用：** 在描述文字中，凡是涉及到代码、库、框架的名字，不要直接写，而是包一层 `<code>` 样式。
- **代码效果：** `font-mono bg-slate-100 text-slate-900 px-1 rounded`。
- **视觉心理：** 这会让技术面试官一眼扫描到他们想要的关键词（如 React, Go），阅读效率极高。

#### 3. 极简的“时间轴”引导

- **视觉细节：** 在左侧的 `●` 圆点下方，画一条淡淡的 1px 线连接到下一个阶段的圆点。
- **落地技巧：** 在 Tailwind 中使用 `before:absolute before:left-[...px] before:top-2 before:bottom-0 before:w-px before:bg-gray-200`。
- **作用：** 它像一个导航，引导读者的视线垂直流动，不会在文字中“迷失”。

---

### 代码示例

根据之前草稿的**“视觉设计方案推导图”**和**“高保真示意图”**逻辑，以下是 `TemplateTechnical.tsx` 的精细化重构示意。

这次重构重点解决了以下问题：

1. **垂直节律 (8px Grid)：** 严格控制了模块间距（`mb-8`）和内部间距（`mt-2`）。
2. **视觉层级：** 强化了姓名与模块标题的对比，弱化了辅助信息（日期）。
3. **技术感细节：** 引入了时间轴线（Timeline）、等宽字体（Monospace）技术标签、以及更精致的图标排版建议。
4. **原子化适配：** 深度结合了你已有的 `useResumeTheme` 钩子。

```tsx
'use client'

import React from 'react'
import { TemplateProps } from './types'
import { useResumeTheme } from './hooks/useResumeTheme'
import { renderDescription, formatDate } from './utils'
import { cn } from '@/lib/utils'
import { Mail, Phone, Github, MapPin, ExternalLink } from 'lucide-react'

/**
 * Technical 模板 - 专为技术岗位打磨
 * 特点：高信息密度、硬朗的垂直节律、强调技术栈标签、时间轴引导
 */
export function TemplateTechnical({
  data,
  config,
  styleConfig,
}: TemplateProps) {
  const { basics, workExperiences, projectExperiences, educations, skills } =
    data
  const theme = useResumeTheme(styleConfig)

  // 辅助组件：模块标题
  const SectionHeader = ({ title }: { title: string }) => (
    <div className="flex items-center mb-4 mt-8 first:mt-0">
      <h3 className="text-[14px] font-bold text-gray-900 uppercase tracking-wider whitespace-nowrap">
        {title}
      </h3>
      <div className="ml-4 h-[2px] w-full bg-gray-900" />
    </div>
  )

  // 辅助组件：时间轴容器
  const TimelineItem = ({
    children,
    isLast = false,
  }: {
    children: React.ReactNode
    isLast?: boolean
  }) => (
    <div className="relative pl-6 pb-6 group">
      {/* 垂直线 */}
      {!isLast && (
        <div className="absolute left-[3px] top-[18px] bottom-0 w-[1px] bg-gray-200 group-hover:bg-gray-400 transition-colors" />
      )}
      {/* 圆点 */}
      <div className="absolute left-0 top-[6px] w-[7px] h-[7px] rounded-full bg-gray-300 border border-white" />
      {children}
    </div>
  )

  const sectionMap: Record<string, React.ReactNode> = {
    basics: null, // 已在 header 处理
    summary: basics.summary && (
      <section style={theme.section}>
        <SectionHeader title="Summary" />
        <p className="text-[13px] leading-relaxed text-gray-600 text-justify">
          {basics.summary}
        </p>
      </section>
    ),
    skills: skills && (
      <section style={theme.section}>
        <SectionHeader title="Technical Skills" />
        <div className="flex flex-wrap gap-2">
          {/* 这里假设 skills 字段可以通过 \n 拆分，或者你可以在数据层支持数组 */}
          {skills.split('\n').map((skillLine, idx) => {
            const [category, items] = skillLine.includes(':')
              ? skillLine.split(':')
              : ['', skillLine]
            return (
              <div key={idx} className="w-full flex items-start gap-2 mb-1">
                {category && (
                  <span className="text-[12px] font-bold text-gray-800 min-w-[70px] mt-1 italic uppercase">
                    {category}:
                  </span>
                )}
                <div className="flex flex-wrap gap-1.5">
                  {items.split(/[，,]/).map((item, i) => (
                    <span
                      key={i}
                      className="px-2 py-0.5 bg-slate-50 border border-slate-200 text-slate-700 text-[11px] font-mono rounded-sm"
                    >
                      {item.trim()}
                    </span>
                  ))}
                </div>
              </div>
            )
          })}
        </div>
      </section>
    ),
    workExperiences: workExperiences?.length > 0 && (
      <section style={theme.section}>
        <SectionHeader title="Experience" />
        <div className="mt-2">
          {workExperiences.map((item, idx) => (
            <TimelineItem
              key={item.id}
              isLast={idx === workExperiences.length - 1}
            >
              <div className="flex justify-between items-start mb-1">
                <div>
                  <span className="text-[14px] font-bold text-gray-900">
                    {item.position}
                  </span>
                  <span className="mx-2 text-gray-300">|</span>
                  <span className="text-[14px] font-semibold text-gray-700">
                    {item.company}
                  </span>
                </div>
                <span className="text-[12px] font-mono text-gray-500 tabular-nums italic">
                  {formatDate(item.startDate)} —{' '}
                  {item.endDate ? formatDate(item.endDate) : 'Present'}
                </span>
              </div>
              <div className="text-[13px] text-gray-600">
                {renderDescription(item.description)}
              </div>
            </TimelineItem>
          ))}
        </div>
      </section>
    ),
    projectExperiences: projectExperiences?.length > 0 && (
      <section style={theme.section}>
        <SectionHeader title="Projects" />
        <div className="space-y-6">
          {projectExperiences.map((item) => (
            <div key={item.id}>
              <div className="flex justify-between items-baseline mb-1">
                <div className="flex items-center gap-2">
                  <h4 className="font-bold text-[14px] text-gray-900">
                    {item.projectName}
                  </h4>
                  <ExternalLink size={12} className="text-gray-400" />
                </div>
                <span className="text-[12px] font-mono text-gray-500 italic">
                  {formatDate(item.startDate)} — {formatDate(item.endDate)}
                </span>
              </div>
              {item.role && (
                <div className="text-[12px] font-semibold text-sky-700 mb-2 uppercase tracking-tight">
                  {item.role}
                </div>
              )}
              <div className="text-[13px] text-gray-600">
                {renderDescription(item.description)}
              </div>
            </div>
          ))}
        </div>
      </section>
    ),
    educations: educations?.length > 0 && (
      <section style={theme.section}>
        <SectionHeader title="Education" />
        <div className="space-y-3">
          {educations.map((item) => (
            <div key={item.id} className="flex justify-between items-baseline">
              <div className="flex items-baseline gap-2">
                <span className="font-bold text-[14px] text-gray-900">
                  {item.school}
                </span>
                <span className="text-[13px] text-gray-700">{item.major}</span>
                {item.degree && (
                  <span className="text-[12px] text-gray-500 italic">
                    ({item.degree})
                  </span>
                )}
              </div>
              <span className="text-[12px] font-mono text-gray-500 italic">
                {formatDate(item.startDate)} — {formatDate(item.endDate)}
              </span>
            </div>
          ))}
        </div>
      </section>
    ),
  }

  return (
    <div
      className={cn(
        'bg-white w-full min-h-full transition-all duration-300',
        theme.fontFamilyClass
      )}
      style={theme.container}
    >
      {/* Header: Compact & High Contrast */}
      <header className="mb-6 flex justify-between items-start">
        <div className="flex-1">
          <h1 className="text-[32px] font-bold text-gray-900 leading-none mb-2 tracking-tighter">
            {basics.name}
          </h1>
          <div className="flex flex-wrap items-center gap-x-4 gap-y-1 text-[12px] text-gray-600">
            {basics.email && (
              <div className="flex items-center gap-1">
                <Mail size={12} /> {basics.email}
              </div>
            )}
            {basics.mobile && (
              <div className="flex items-center gap-1">
                <Phone size={12} /> {basics.mobile}
              </div>
            )}
            {basics.location && (
              <div className="flex items-center gap-1">
                <MapPin size={12} /> {basics.location}
              </div>
            )}
          </div>
        </div>

        <div className="flex flex-col items-end gap-1">
          {basics.github && (
            <div className="flex items-center gap-1 text-[12px] font-mono text-gray-900 bg-gray-100 px-2 py-1 rounded">
              <Github size={12} /> github.com/{basics.github}
            </div>
          )}
          <div className="flex gap-2 text-[11px] text-gray-400">
            {basics.wechat && <span>WX: {basics.wechat}</span>}
            {basics.qq && <span>QQ: {basics.qq}</span>}
          </div>
        </div>
      </header>

      {/* Main Content Sections */}
      <div className="flex flex-col">
        {config.order.map((key) => {
          if (config.hidden.includes(key)) return null
          return <div key={key}>{sectionMap[key]}</div>
        })}
      </div>

      {/* Footer / Page Number Placeholder */}
      <footer className="mt-12 pt-4 border-t border-gray-100 text-center">
        <span className="text-[10px] text-gray-300 uppercase tracking-[0.2em]">
          Curriculum Vitae • {basics.name}
        </span>
      </footer>
    </div>
  )
}
```

### 💡 关键代码逻辑解读：

1. **Timeline 动画感逻辑：** \* 通过 `TimelineItem` 组件，利用 Tailwind 的 `absolute` 定位和 `left-0` 成功构建了贯穿“工作经历”的垂直线。

- 使用了 `group-hover` 伪类，虽然是打印模板，但在预览时会增加交互感。

2. **Tech Stack (技能标签) 优化：**

- 代码逻辑会尝试识别技能描述中的冒号（如 `Frontend: React, Next.js`），并自动将其转换为“分类标签墙”。
- 使用了 `font-mono` 字体来突出技术名词，这符合技术岗位的审美习惯。

3. **精确的字体配置：**

- 姓名直接用了 `text-[32px]`，利用 `tracking-tighter` 制造大厂简历的厚重感。
- 模块标题采用了 `uppercase tracking-wider` 配合横线（`ml-4 h-[2px] w-full bg-gray-900`），极大地提升了扫视效率。

4. **图标化联系方式：**

- 引入了 `lucide-react` 图标（需确保项目中已安装），解决了原版文字排版散乱的问题，使 Header 显得更专业。

---

## 字体选择

目前系统已经支持了 `Roboto`、`Open Sans`、`Lato`、`JetBrains Mono`、`Noto Serif` 和 `Playfair Display` 这几种字体。

对于升级后的 **Technical (技术型)** 模板，我建议**有针对性地新增或优化 1-2 款字体**。理由如下：

---

### 1. 现状评估：现有字体是否够用？

- **JetBrains Mono (已支持)：** 这是一个极佳的技术字体。它非常适合作为技术栈（Skills）或代码片段的装饰性排版。
- **Roboto / Open Sans (已支持)：** 虽然稳妥，但在技术审美中显得略微“通用”且“陈旧”。对于追求现代互联网（Startup/Modern Tech）感的简历，它们的 **x-height（小写字母高度）** 表现稍显普通。

### 2. 建议新增的适配字体（Google Fonts 免费方案）

为了让技术模板达到“脱胎换骨”的精细度，我推荐引入以下两款字体：

#### A. **Inter** (无衬线体 - 现代技术/互联网的首选)

- **推荐理由：** 这是目前互联网大厂（如 GitHub、Figma、Notion）几乎标配的 UI 字体。它专门为屏幕阅读和**小字号（Small Text）**优化过，具有极高的可读性。
- **应用场景：** 技术简历的正文和职位标题。它能让你的简历看起来像一份极简、现代的网页，非常有“硅谷范儿”。
- **Google Fonts:** [Inter](https://fonts.google.com/specimen/Inter)

#### B. **IBM Plex Mono** (等宽体 - 工业感与理性的极致)

- **推荐理由：** 相比 `JetBrains Mono` 的圆润，`IBM Plex Mono` 更加硬朗、理性，具有强烈的工程师气质。
- **应用场景：** 用于替换或作为 `JetBrains Mono` 的进阶选项，处理日期、技术名词或项目链接。
- **Google Fonts:** [IBM Plex Mono](https://fonts.google.com/specimen/IBM+Plex+Mono)

---

### 3. 如何在架构上优雅落地？

为了配合商业化限制，你不需要为每个模板加载所有字体。你可以按照以下步骤微调 `useResumeTheme.ts`：

1. **在 `getFontFamilyClass` 中增加 `inter` 分支：**

```typescript
case 'inter':
  return 'font-[family-name:var(--font-inter),sans-serif]'

```

2. **设定 Technical 模板的默认配置：**
   在 `TemplateTechnical` 渲染时，如果用户未指定字体，默认将其 `fontFamily` 设为 `inter` 或 `jetbrains-mono`。
