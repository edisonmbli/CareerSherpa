## Standard & Elegant 模板二次重构

---

### 1. TemplateStandard (标准通用) 重构方案

**诊断：** “素颜感”太强。现在的 Standard 就像是 `Notepad` 打印出来的，缺乏“文档的庄重感”。
**设计目标：** **“现代信笺 (Modern Letterhead)”**。
我们要保留它的克制，但通过增加**结构化的灰度背景**和**明确的分割线**，让它看起来像是一份精心排版的正式公文，而不是草稿。

#### ❌ 问题点 (Before)

- 全白背景，信息漂浮。
- 线条太短，视觉引导力弱。
- 头部信息散落在页面上，没有“成组”。

#### ✅ 优化策略 (After)

1. **引入“刊头”概念 (Header Block)：** 给头部增加极淡的背景色（如 `bg-slate-50`），或者在名字下方增加全宽的分割线，建立“页眉”的即视感。
2. **结构化联系栏：** 将联系方式整理到一个深色或灰色的横条中，或者使用网格对齐，增加“秩序感”。
3. **强化标题层级：** 章节标题不再只是文字+短线，改为**“文字 + 全宽细线”**，让视觉视线能贯穿整个页面宽度，填满 A4 纸的横向空间。

---

### 2. TemplateElegant (现代优稚) 重构方案

**诊断：** “对齐混乱”。目前的 Elegant 试图做居中，但正文又是左对齐，导致视觉重心在“摆动”，且纯文本缺乏“仪式感”。
**设计目标：** **“艺术画框 (The Art Frame)”**。
优雅不仅仅是居中，而是**“留白”**和**“衬线体”**的完美结合。我们需要用边框和特殊的符号来营造高级感。

#### ❌ 问题点 (Before)

- **轴线打架：** 标题居中，经历里的公司名左对齐，日期右对齐 -> 视线乱跳。
- **缺乏装饰：** 只有文字，没有图形语言来支撑“优雅”。

#### ✅ 优化策略 (After)

1. **统一的中轴线 (The Central Spine)：**

- **核心法则：** 所有的 **元数据**（公司、职位、学校、时间）全部**居中对齐**。
- **正文处理：** 只有具体的 Bullet Points 保持左对齐（为了易读），但这个文本块本身要**限制宽度（如 `max-w-2xl`）并整体居中**。

2. **画框设计 (Border)：** 在页面四周增加双线边框 (`border-double`) 或内缩的单线边框，营造“请柬”或“菜单”的高级感。
3. **微排版装饰：** 使用 `♦` (菱形) 或 `~` (波浪) 代替普通的 `•` (圆点) 或 `|` (竖线)。

---

### 🛠️ 代码落地指南

以下是针对这两个模板的重构代码示例，请参考。

#### 1. 重构 `TemplateStandard.tsx`

**主要改动：**

- **Header:** 增加了 `border-b-2` 和更紧凑的排版，让其看起来像正式信头。
- **Section:** 标题改为全宽线条分割，增加了 `bg-gray-50` 的微弱背景条增强层级。
- **Contact:** 整理为一行紧凑的元数据流。

```tsx
'use client'

import React from 'react'
import { TemplateProps, TemplateConfig } from './types'
import { useResumeTheme } from './hooks/useResumeTheme'
import {
  renderDescription,
  formatDate,
  getSectionTitle,
  renderSocialItem,
} from './utils'
import { cn } from '@/lib/utils'
import { Mail, Phone, MapPin, Github, Globe, Link2 } from 'lucide-react'
import { InteractiveSection } from './InteractiveSection'

/**
 * Standard 模板 v2.0 - 现代信笺型
 * 优化点：
 * 1. 增强 Header 的“信头感”，增加全宽分割线。
 * 2. 章节标题改为“全宽下划线”，消除“内容太少”的视觉空旷感。
 * 3. 强调职位/学校 (Bold) 甚于公司/时间，符合 HR 扫视逻辑。
 */
export function TemplateStandard({ data, config, styleConfig }: TemplateProps) {
  const {
    basics,
    workExperiences,
    projectExperiences,
    educations,
    skills,
    certificates,
    hobbies,
    customSections,
    sectionTitles,
  } = data
  const theme = useResumeTheme({
    themeColor: '#374151',
    ...styleConfig,
  })

  // 辅助组件：全宽标题 (带下划线)
  const SectionHeader = ({ title }: { title: string }) => (
    <div className="mb-4 mt-6 first:mt-2 break-after-avoid">
      <div className="flex items-center gap-4">
        <h3
          className="font-bold text-gray-900 uppercase tracking-widest whitespace-nowrap"
          style={{ fontSize: '1em', color: theme.themeColor }}
        >
          {title}
        </h3>
        <div className="h-[1px] bg-gray-200 flex-1" />
      </div>
    </div>
  )

  const sectionMap: Record<string, React.ReactNode> = {
    basics: null,
    summary: basics.summary && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="summary">
          <SectionHeader
            title={getSectionTitle(
              'summary',
              basics.lang,
              sectionTitles?.['summary']
            )}
          />
          <div
            className="leading-relaxed text-gray-700 text-justify"
            style={{ fontSize: '0.95em' }}
          >
            {renderDescription(basics.summary)}
          </div>
        </InteractiveSection>
      </section>
    ),
    skills: skills && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="skills">
          <SectionHeader
            title={getSectionTitle(
              'skills',
              basics.lang,
              sectionTitles?.['skills']
            )}
          />
          <div
            className="text-gray-700 leading-relaxed bg-gray-50 p-3 rounded-sm border border-gray-100"
            style={{ fontSize: '0.93em' }}
          >
            {/* 技能改为 Grid 布局或 Flex 换行，增加一点填充感 */}
            <div className="flex flex-wrap gap-x-6 gap-y-2">
              {skills.split('\n').map((skill, i) => (
                <div key={i} className="flex items-center gap-2">
                  <div
                    className="w-1.5 h-1.5 rounded-full opacity-60"
                    style={{ backgroundColor: theme.themeColor }}
                  />
                  <span>{skill.trim().replace(/^[-•]\s*/, '')}</span>
                </div>
              ))}
            </div>
          </div>
        </InteractiveSection>
      </section>
    ),
    workExperiences: workExperiences?.length > 0 && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="workExperiences">
          <SectionHeader
            title={getSectionTitle(
              'workExperiences',
              basics.lang,
              sectionTitles?.['workExperiences']
            )}
          />
        </InteractiveSection>
        <div className="space-y-5">
          {workExperiences.map((item) => (
            <div key={item.id} className="page-break-fix group">
              <InteractiveSection sectionKey="workExperiences" itemId={item.id}>
                {/* 第一行：公司 + 时间 (两端对齐) */}
                <div className="flex justify-between items-baseline mb-0.5">
                  <h4
                    className="font-bold text-gray-900"
                    style={{ fontSize: '1.1em' }}
                  >
                    {item.company}
                  </h4>
                  <span
                    className="font-mono text-gray-500 tabular-nums text-sm shrink-0"
                    style={{ fontSize: '0.85em' }}
                  >
                    {formatDate(item.startDate)} —{' '}
                    {item.endDate ? formatDate(item.endDate) : 'Present'}
                  </span>
                </div>

                {/* 第二行：职位 (主色调) */}
                <div
                  className="mb-2 font-semibold"
                  style={{ color: theme.themeColor, fontSize: '0.95em' }}
                >
                  {item.position}
                </div>

                <div
                  className="text-gray-700 leading-normal"
                  style={{ fontSize: '0.93em' }}
                >
                  {renderDescription(item.description)}
                </div>
              </InteractiveSection>
            </div>
          ))}
        </div>
      </section>
    ),
    projectExperiences: projectExperiences?.length > 0 && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="projectExperiences">
          <SectionHeader
            title={getSectionTitle(
              'projectExperiences',
              basics.lang,
              sectionTitles?.['projectExperiences']
            )}
          />
        </InteractiveSection>
        <div className="space-y-5">
          {projectExperiences.map((item) => (
            <div key={item.id} className="page-break-fix">
              <InteractiveSection
                sectionKey="projectExperiences"
                itemId={item.id}
              >
                <div className="flex justify-between items-baseline mb-0.5">
                  <h4
                    className="font-bold text-gray-900"
                    style={{ fontSize: '1.05em' }}
                  >
                    {item.projectName}
                  </h4>
                  <span
                    className="font-mono text-gray-500 tabular-nums text-sm shrink-0"
                    style={{ fontSize: '0.85em' }}
                  >
                    {formatDate(item.startDate)} — {formatDate(item.endDate)}
                  </span>
                </div>

                <div className="flex flex-wrap items-center gap-3 mb-2 text-sm">
                  {item.role && (
                    <span
                      className="font-semibold"
                      style={{ color: theme.themeColor }}
                    >
                      {item.role}
                    </span>
                  )}
                  {(item.githubUrl || item.demoUrl) && (
                    <span className="text-gray-300">|</span>
                  )}
                  {item.demoUrl && (
                    <a
                      href={item.demoUrl}
                      target="_blank"
                      className="flex items-center gap-1 text-gray-500 hover:text-gray-900 hover:underline"
                    >
                      <Link2 size={12} /> Demo
                    </a>
                  )}
                  {item.githubUrl && (
                    <a
                      href={item.githubUrl}
                      target="_blank"
                      className="flex items-center gap-1 text-gray-500 hover:text-gray-900 hover:underline"
                    >
                      <Github size={12} /> Code
                    </a>
                  )}
                </div>

                <div className="text-gray-700" style={{ fontSize: '0.93em' }}>
                  {renderDescription(item.description)}
                </div>
              </InteractiveSection>
            </div>
          ))}
        </div>
      </section>
    ),
    educations: educations?.length > 0 && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="educations">
          <SectionHeader
            title={getSectionTitle(
              'educations',
              basics.lang,
              sectionTitles?.['educations']
            )}
          />
        </InteractiveSection>
        <div className="space-y-3">
          {educations.map((item) => (
            <div key={item.id} className="page-break-fix">
              <InteractiveSection sectionKey="educations" itemId={item.id}>
                <div className="flex justify-between items-start">
                  <div>
                    <h4
                      className="font-bold text-gray-900"
                      style={{ fontSize: '1em' }}
                    >
                      {item.school}
                    </h4>
                    <div
                      className="text-gray-600 mt-0.5"
                      style={{ fontSize: '0.93em' }}
                    >
                      {item.major} {item.degree && `· ${item.degree}`}
                    </div>
                  </div>
                  <span
                    className="font-mono text-gray-500 tabular-nums text-sm text-right shrink-0"
                    style={{ fontSize: '0.85em' }}
                  >
                    {formatDate(item.startDate)} — {formatDate(item.endDate)}
                  </span>
                </div>
                {item.description && (
                  <div className="mt-1 text-gray-500 text-sm">
                    {item.description}
                  </div>
                )}
              </InteractiveSection>
            </div>
          ))}
        </div>
      </section>
    ),
    certificates: certificates && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="certificates">
          <SectionHeader
            title={getSectionTitle(
              'certificates',
              basics.lang,
              sectionTitles?.['certificates']
            )}
          />
          <div
            className="text-gray-700 leading-relaxed"
            style={{ fontSize: '0.93em' }}
          >
            {renderDescription(certificates)}
          </div>
        </InteractiveSection>
      </section>
    ),
    hobbies: hobbies && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="hobbies">
          <SectionHeader
            title={getSectionTitle(
              'hobbies',
              basics.lang,
              sectionTitles?.['hobbies']
            )}
          />
          <div
            className="text-gray-700 leading-relaxed"
            style={{ fontSize: '0.93em' }}
          >
            {renderDescription(hobbies)}
          </div>
        </InteractiveSection>
      </section>
    ),
    customSections: customSections?.length > 0 && (
      <>
        {customSections.map((item) => (
          <section key={item.id} style={theme.section}>
            <InteractiveSection sectionKey="customSections" itemId={item.id}>
              <SectionHeader title={item.title || 'Untitled'} />
              <div
                className="text-gray-700 leading-relaxed"
                style={{ fontSize: '0.93em' }}
              >
                {renderDescription(item.description)}
              </div>
            </InteractiveSection>
          </section>
        ))}
      </>
    ),
  }

  return (
    <div
      className={cn(
        'bg-white w-full min-h-full transition-all duration-300 relative',
        theme.fontFamilyClass
      )}
      style={theme.container}
    >
      {/* 顶部装饰条: 增加“信笺”的正式感 */}
      <div
        className="h-2 w-full absolute top-0 left-0"
        style={{ backgroundColor: theme.themeColor }}
      />

      {/* Header: 更加结构化的布局 */}
      <InteractiveSection sectionKey="basics">
        <header className="mb-8 pt-8 pb-6 border-b border-gray-200">
          <div className="flex flex-col md:flex-row justify-between items-start gap-6">
            <div className="flex-1">
              <h1
                className="font-black text-gray-900 tracking-tight leading-tight mb-3 uppercase"
                style={{ fontSize: '2.5em' }}
              >
                {basics.name}
              </h1>

              {/* 个人简介/Title (如果有的话) - 暂时用 Summary 的第一句代替或者留空 */}
              {/* <p className="text-gray-500 font-medium text-lg">Product Manager</p> */}

              <div className="flex flex-wrap gap-x-6 gap-y-2 text-sm text-gray-600 mt-2 font-medium">
                {basics.mobile && (
                  <div className="flex items-center gap-1.5">
                    <Phone size={14} className="text-gray-400" />{' '}
                    {basics.mobile}
                  </div>
                )}
                {basics.email && (
                  <div className="flex items-center gap-1.5">
                    <Mail size={14} className="text-gray-400" /> {basics.email}
                  </div>
                )}
                {(basics.address || basics.location) && (
                  <div className="flex items-center gap-1.5">
                    <MapPin size={14} className="text-gray-400" />{' '}
                    {basics.address || basics.location}
                  </div>
                )}
              </div>

              {/* 社交链接行 */}
              <div className="flex flex-wrap gap-x-5 gap-y-2 mt-3 text-xs text-gray-500">
                {[
                  'website',
                  'github',
                  'linkedin',
                  'twitter',
                  'dribbble',
                  'behance',
                ].map((key) => {
                  const val = basics[key as keyof typeof basics]
                  if (!val) return null
                  const social = renderSocialItem(key, val)
                  if (!social) return null
                  const { href, icon: Icon, displayLabel } = social
                  return (
                    <a
                      key={key}
                      href={href}
                      target="_blank"
                      rel="noreferrer"
                      className="flex items-center gap-1.5 hover:text-gray-900 hover:underline transition-colors"
                    >
                      <Icon size={12} />
                      <span>{displayLabel}</span>
                    </a>
                  )
                })}
              </div>
            </div>

            {/* Avatar */}
            {basics.photoUrl && (
              <div className="shrink-0">
                <img
                  src={basics.photoUrl}
                  alt={basics.name}
                  className="w-24 h-24 rounded-lg object-cover border border-gray-200 shadow-sm"
                />
              </div>
            )}
          </div>
        </header>
      </InteractiveSection>

      {/* Main Sections Body */}
      <div className="flex flex-col">
        {config.order.map((key) => {
          if (
            config.hidden.includes(key) ||
            key === 'basics' ||
            !sectionMap[key]
          )
            return null
          return <div key={key}>{sectionMap[key]}</div>
        })}
      </div>

      {/* Footer */}
      <footer className="mt-8 pt-4 border-t border-gray-100 flex justify-between items-center text-xs text-gray-400 font-mono">
        <span>{new Date().getFullYear()} Resume</span>
        <span>Generated by CareerShaper</span>
      </footer>
    </div>
  )
}

export const StandardDefaults: TemplateConfig = {
  themeColor: '#374151', // Charcoal
  fontFamily: 'roboto',
  fontSize: 1,
  baseFontSize: 14,
  lineHeight: 1.6, // 略微增加行高以提升文档呼吸感
  pageMargin: 12, // 标准 12mm 边距
  sectionSpacing: 24,
  itemSpacing: 12,
}
```

#### 2. 重构 `TemplateElegant.tsx`

**主要改动：**

- **外框 (Border):** 增加了一个 `border-[1px]` 内缩边框，立刻有了“菜单/证书”的高级感。
- **完全居中对齐 (Centered Alignment):** 所有的元数据（公司、职位、时间）全部强制居中。
- **装饰符号:** 使用 `♦` 符号作为视觉锚点。
- **内容限制宽度:** 正文虽然左对齐（为了阅读），但被包裹在一个 `max-w-xl mx-auto` 容器中，保持了整体的纤细感。

```tsx
/* eslint-disable @next/next/no-img-element */
'use client'

import React from 'react'
import { TemplateProps, TemplateConfig } from './types'
import { useResumeTheme } from './hooks/useResumeTheme'
import {
  renderDescription,
  formatDate,
  getSectionTitle,
  renderSocialItem,
} from './utils'
import { cn } from '@/lib/utils'
import { Mail, Phone, MapPin, ExternalLink } from 'lucide-react'
import { InteractiveSection } from './InteractiveSection'

/**
 * Elegant 模板 v2.0 - 艺术画框型
 * 优化点：
 * 1. 彻底贯彻“中轴对称”美学，所有 Header/Meta 居中。
 * 2. 增加内缩边框 (Inset Border)，营造画框/请柬的高级感。
 * 3. 字体与留白：衬线体 + 极大的段间距。
 * 4. 符号装饰：使用菱形 ♦ 代替圆点。
 */
export function TemplateElegant({ data, config, styleConfig }: TemplateProps) {
  const {
    basics,
    workExperiences,
    projectExperiences,
    educations,
    skills,
    certificates,
    hobbies,
    customSections,
    sectionTitles,
  } = data
  const theme = useResumeTheme(styleConfig)

  // 辅助组件：雅致标题 (带装饰性符号)
  const SectionHeader = ({ title }: { title: string }) => (
    <div className="flex flex-col items-center mb-8 mt-8 first:mt-4 break-after-avoid">
      <div className="flex items-center gap-3 opacity-60 mb-2">
        <div className="h-[1px] w-8 bg-gray-400" />
        <span className="text-[10px] text-gray-400">♦</span>
        <div className="h-[1px] w-8 bg-gray-400" />
      </div>
      <h3
        className="text-[1.2em] font-serif font-bold text-gray-900 tracking-[0.25em] uppercase text-center"
        style={{ color: theme.themeColor }}
      >
        {title}
      </h3>
    </div>
  )

  // 辅助组件：优雅的内容描述容器 (限制宽度以保持阅读美感)
  const Description = ({
    content,
    className,
    center = false,
  }: {
    content?: string | undefined
    className?: string
    center?: boolean
  }) => {
    if (!content) return null
    return (
      <div
        className={cn(
          'text-gray-600 leading-loose font-light mx-auto max-w-xl', // 关键：限制宽度 + 居中
          center ? 'text-center' : 'text-justify',
          // 自定义列表样式：极细
          '[&_ul]:list-none [&_ul]:pl-0',
          '[&_li]:relative [&_li]:pl-0 [&_li]:mb-1',
          center ? '' : '[&_li]:text-justify',
          className
        )}
        style={theme.text}
      >
        {renderDescription(content)}
      </div>
    )
  }

  const sectionMap: Record<string, React.ReactNode> = {
    basics: null,
    summary: basics.summary && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="summary">
          <div className="flex flex-col items-center">
            <SectionHeader
              title={getSectionTitle(
                'summary',
                basics.lang,
                sectionTitles?.['summary']
              )}
            />
            <p
              className="leading-[2] text-gray-600 text-center max-w-lg mx-auto font-serif italic"
              style={theme.text}
            >
              &ldquo; {basics.summary} &rdquo;
            </p>
          </div>
        </InteractiveSection>
      </section>
    ),
    skills: skills && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="skills">
          <div>
            <SectionHeader
              title={getSectionTitle(
                'skills',
                basics.lang,
                sectionTitles?.['skills']
              )}
            />
            <div
              className="flex flex-wrap justify-center gap-x-8 gap-y-3 leading-relaxed text-gray-700 text-center max-w-2xl mx-auto font-serif"
              style={theme.text}
            >
              {skills.split('\n').map((skill, idx) => (
                <span key={idx} className="relative inline-block">
                  {skill.trim().replace(/^[-•]\s*/, '')}
                </span>
              ))}
            </div>
          </div>
        </InteractiveSection>
      </section>
    ),
    workExperiences: workExperiences?.length > 0 && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="workExperiences">
          <SectionHeader
            title={getSectionTitle(
              'workExperiences',
              basics.lang,
              sectionTitles?.['workExperiences']
            )}
          />
        </InteractiveSection>
        <div className="space-y-10">
          {workExperiences.map((item) => (
            <div key={item.id} className="group page-break-fix">
              <InteractiveSection sectionKey="workExperiences" itemId={item.id}>
                <div className="flex flex-col items-center text-center mb-4">
                  <h4
                    className="font-bold text-gray-900 tracking-wider text-lg"
                    style={{ fontFamily: 'var(--font-serif)' }}
                  >
                    {item.company}
                  </h4>

                  <div className="flex items-center gap-2 mt-1 text-sm text-gray-500 font-serif italic">
                    <span style={{ color: theme.themeColor }}>
                      {item.position}
                    </span>
                    <span>|</span>
                    <span className="tabular-nums tracking-widest">
                      {formatDate(item.startDate)} —{' '}
                      {item.endDate ? formatDate(item.endDate) : 'Present'}
                    </span>
                  </div>
                </div>

                <Description content={item.description} className="mt-2" />
              </InteractiveSection>
            </div>
          ))}
        </div>
      </section>
    ),
    projectExperiences: projectExperiences?.length > 0 && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="projectExperiences">
          <SectionHeader
            title={getSectionTitle(
              'projectExperiences',
              basics.lang,
              sectionTitles?.['projectExperiences']
            )}
          />
        </InteractiveSection>
        <div className="space-y-8">
          {projectExperiences.map((item) => (
            <div key={item.id} className="page-break-fix">
              <InteractiveSection
                sectionKey="projectExperiences"
                itemId={item.id}
              >
                <div className="flex flex-col items-center text-center mb-3">
                  <h4
                    className="font-bold text-gray-900 tracking-wide text-lg"
                    style={{ fontFamily: 'var(--font-serif)' }}
                  >
                    {item.projectName}
                  </h4>

                  <div className="flex items-center gap-3 mt-1 text-sm text-gray-500">
                    {item.role && (
                      <span
                        className="italic"
                        style={{ color: theme.themeColor }}
                      >
                        {item.role}
                      </span>
                    )}
                    <span className="text-gray-300">/</span>
                    <span className="tabular-nums tracking-widest">
                      {formatDate(item.startDate)} — {formatDate(item.endDate)}
                    </span>
                  </div>

                  {/* Project Links */}
                  {(item.githubUrl || item.demoUrl) && (
                    <div className="flex gap-4 mt-2 text-xs uppercase tracking-widest text-gray-400">
                      {item.demoUrl && (
                        <a
                          href={item.demoUrl}
                          target="_blank"
                          className="hover:text-gray-900 border-b border-transparent hover:border-gray-900 transition-all"
                        >
                          Live Demo
                        </a>
                      )}
                      {item.githubUrl && (
                        <a
                          href={item.githubUrl}
                          target="_blank"
                          className="hover:text-gray-900 border-b border-transparent hover:border-gray-900 transition-all"
                        >
                          Source Code
                        </a>
                      )}
                    </div>
                  )}
                </div>

                <Description content={item.description} />
              </InteractiveSection>
            </div>
          ))}
        </div>
      </section>
    ),
    educations: educations?.length > 0 && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="educations">
          <SectionHeader
            title={getSectionTitle(
              'educations',
              basics.lang,
              sectionTitles?.['educations']
            )}
          />
        </InteractiveSection>
        <div className="space-y-6">
          {educations.map((item) => (
            <div key={item.id} className="page-break-fix">
              <InteractiveSection sectionKey="educations" itemId={item.id}>
                <div className="flex flex-col items-center text-center">
                  <span
                    className="font-bold text-gray-900 text-lg tracking-wide"
                    style={{ fontFamily: 'var(--font-serif)' }}
                  >
                    {item.school}
                  </span>
                  <div className="text-gray-600 mt-1 mb-1 font-serif italic">
                    {item.major} {item.degree && `· ${item.degree}`}
                  </div>
                  <span className="text-gray-400 text-xs tracking-[0.2em] uppercase">
                    {formatDate(item.startDate)} — {formatDate(item.endDate)}
                  </span>
                </div>
              </InteractiveSection>
            </div>
          ))}
        </div>
      </section>
    ),
    certificates: certificates && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="certificates">
          <div>
            <SectionHeader
              title={getSectionTitle(
                'certificates',
                basics.lang,
                sectionTitles?.['certificates']
              )}
            />
            <Description content={certificates} center />
          </div>
        </InteractiveSection>
      </section>
    ),
    hobbies: hobbies && (
      <section style={theme.section}>
        <InteractiveSection sectionKey="hobbies">
          <div>
            <SectionHeader
              title={getSectionTitle(
                'hobbies',
                basics.lang,
                sectionTitles?.['hobbies']
              )}
            />
            <Description content={hobbies} center />
          </div>
        </InteractiveSection>
      </section>
    ),
    customSections: customSections?.length > 0 && (
      <InteractiveSection sectionKey="customSections">
        <>
          {customSections.map((item) => (
            <section key={item.id} style={theme.section}>
              <div className="flex flex-col">
                <SectionHeader title={item.title || 'Untitled'} />
                <Description content={item.description} center />
              </div>
            </section>
          ))}
        </>
      </InteractiveSection>
    ),
  }

  return (
    <div
      className={cn(
        'bg-white w-full min-h-full transition-all duration-300 font-serif relative p-8', // 增加内边距给边框留位置
        theme.fontFamilyClass
      )}
      style={theme.container}
    >
      {/* 装饰性画框 (Inset Border) */}
      <div
        className="absolute inset-4 border border-gray-800 pointer-events-none"
        style={{
          borderWidth: '1px',
          borderColor: theme.themeColor,
          opacity: 0.2,
        }}
      />
      <div
        className="absolute inset-5 border border-gray-800 pointer-events-none"
        style={{
          borderWidth: '1px',
          borderColor: theme.themeColor,
          opacity: 0.1,
        }}
      />

      {/* Header: Absolute Center */}
      <InteractiveSection sectionKey="basics">
        <header className="mb-12 flex flex-col items-center text-center pt-10 relative z-10">
          {/* Photo (Optional) - Centered */}
          {basics.photoUrl && (
            <div className="mb-6 w-32 h-32 rounded-full overflow-hidden border border-gray-200 p-1 bg-white">
              <img
                src={basics.photoUrl}
                alt={basics.name}
                className="w-full h-full object-cover rounded-full grayscale hover:grayscale-0 transition-all duration-500"
              />
            </div>
          )}

          <h1
            className="font-serif font-bold tracking-[0.15em] mb-4 text-gray-900 uppercase"
            style={{ fontSize: '2.2em' }}
          >
            {basics.name}
          </h1>

          <div className="flex flex-wrap justify-center items-center gap-x-6 gap-y-2 text-gray-500 text-sm tracking-wider px-8 font-light">
            {[basics.mobile, basics.email, basics.address || basics.location]
              .filter(Boolean)
              .map((val, i) => (
                <React.Fragment key={i}>
                  {i > 0 && (
                    <span className="text-gray-300 text-[10px]">♦</span>
                  )}
                  <span>{val}</span>
                </React.Fragment>
              ))}
          </div>

          <div className="flex flex-wrap justify-center gap-4 mt-4 text-xs tracking-widest text-gray-400 uppercase">
            {['website', 'github', 'linkedin', 'dribbble', 'behance'].map(
              (key) => {
                const val = basics[key as keyof typeof basics]
                if (!val) return null
                const social = renderSocialItem(key, val)
                if (!social) return null
                return (
                  <a
                    key={key}
                    href={social.href}
                    target="_blank"
                    rel="noreferrer"
                    className="hover:text-gray-900 border-b border-transparent hover:border-gray-900 transition-all"
                  >
                    {key}
                  </a>
                )
              }
            )}
          </div>
        </header>
      </InteractiveSection>

      {/* Main Content Sections */}
      <div className="flex flex-col relative z-10 px-4 md:px-8">
        {config.order.map((key) => {
          if (config.hidden.includes(key) || key === 'basics') return null
          return <React.Fragment key={key}>{sectionMap[key]}</React.Fragment>
        })}
      </div>

      {/* Minimal Footer */}
      <footer className="mt-16 pt-8 pb-4 flex justify-center items-center font-serif italic text-slate-300 text-xs">
        <span>— {new Date().getFullYear()} —</span>
      </footer>
    </div>
  )
}

export const ElegantDefaults: TemplateConfig = {
  themeColor: '#27272a', // Zinc 800 (Classic Black/Dark Grey)
  fontFamily: 'playfair', // Playfair Display
  fontSize: 1,
  baseFontSize: 13.5,
  lineHeight: 1.8, // 极大的行高
  pageMargin: 16, // 宽边距
  sectionSpacing: 32, // 宽段落间距
  itemSpacing: 16,
}
```
