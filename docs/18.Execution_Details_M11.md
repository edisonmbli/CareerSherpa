### M11 落地指南 (致 Agentic Coder)

**里程碑目标**：完成 MVP 收尾，交付一个“简洁、高效、高水平”的 `Landing Page`，并完成 SEO、Analytics 和移动端 适配。

**核心规范**：

- **UI/UX**：严格遵循 M3（设计系统）和 `Refactoring UI` 原则（留白、排版层级、限制色彩）。
- **技术**：`app/[locale]/page.tsx`/page.tsx] 必须是 Server Component (SSR)，以实现 M10 规划的 SEO。

---

#### 任务 1：(AI IDE) `Landing Page` 页面骨架

- **目标**：创建 `app/[locale]/page.tsx`/page.tsx] 文件，并设置好 i18n 和 M3 规范。
- **文件**：`app/[locale]/page.tsx`

<!-- end list -->

```typescript
// AI IDE: Create this file at app/[locale]/page.tsx
import { Locale } from '@/i18n-config'
import { getDictionary } from '@/lib/i18n/dictionaries' // M2 成果
import { HeroSection } from '@/components/landing/HeroSection' // M11.2
import { ValuePropsSection } from '@/components/landing/ValuePropsSection' // M11.2
import { HowItWorksSection } from '@/components/landing/HowItWorksSection' // M11.2
import { FaqSection } from '@/components/landing/FaqSection' // M11.2
import { CtaSection } from '@/components/landing/CtaSection' // M11.2
import { trackEvent } from '@/lib/analytics' // M7 成果

// M10 SEO 任务：实现 M10 规划的 generateMetadata
export async function generateMetadata({
  params: { locale },
}: {
  params: { locale: Locale }
}) {
  const dict = (await getDictionary(locale)).landing.seo
  const siteUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000'

  return {
    title: dict.title, // "AI 求职助手 | 帮您定制简历与面试"
    description: dict.description, // "三步拿到更高匹配度的面试机会..."
    metadataBase: new URL(siteUrl),
    alternates: {
      canonical: `/${locale}`,
      languages: {
        en: '/en',
        zh: '/zh',
      },
    },
    // (AI IDE: M10 在此添加 OpenGraph, Twitter card 标签)
  }
}

export default async function LandingPage({
  params: { locale },
}: {
  params: { locale: Locale }
}) {
  const dict = (await getDictionary(locale)).landing

  // M7 Analytics 埋点 (服务端)
  // (注意：这只追踪 SSR，M11.5 会补充客户端 Vercel Analytics)
  trackEvent(null, 'PAGE_VIEW_LANDING', { locale: locale })

  return (
    <div className="flex flex-col items-center overflow-x-hidden">
      {/* 规范：
        1. AI IDE 必须将页面拆分为 M3 规范的 Client Components。
        2. 每个 Section 必须使用 <section> 标签，并应用 M3 的间距规范
           (e.g., `className="container mx-auto px-4 py-16 lg:py-24"`)
      */}

      {/* M11.2 Hero: 必须使用 M3 字体规范 和你提供的 `image_49c2ea.jpg` */}
      <HeroSection dict={dict.hero} />

      {/* M11.2 Value Props: 必须使用 M3 的 `AppCard` 组件 */}
      <ValuePropsSection dict={dict.valueProps} />

      {/* M11.2 How it Works: 必须使用 M3 规范 */}
      <HowItWorksSection dict={dict.howItWorks} />

      {/* M11.2 FAQ: 必须使用 shadcn/ui 的 `Accordion` 组件 */}
      <FaqSection dict={dict.faq} />

      {/* M11.2 CTA: 必须使用 M3 的 `Button (primary, size='lg')` */}
      <CtaSection dict={dict.cta} />

      {/* M10 SEO 任务 (JSON-LD) */}
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{
          __html: JSON.stringify({
            '@context': 'https://schema.org',
            '@type': 'SoftwareApplication',
            name: 'AI 求职助手 (Job Assistant AI)',
            operatingSystem: 'Web',
            applicationCategory: 'BusinessApplication',
            description: dict.seo.description,
            offers: {
              '@type': 'Offer',
              price: '0', // (免费试用)
              priceCurrency: 'CNY',
            },
          }),
        }}
      />
    </div>
  )
}
```

---

#### 任务 2：(AI IDE) `HeroSection` 组件 (M11.2)

- **目标**：实现“高水平”的视觉第一印象。
- **文件**：`components/landing/HeroSection.tsx`
- **规范**：
  1.  **(M3 字体)**：`h1` 标题 **必须**使用 `text-4xl md:text-5xl lg:text-6xl`、`font-bold`、`tracking-tight`、`leading-tight`。
  2.  **(M3 字体)**：副标题 **必须**使用 `text-lg md:text-xl`、`text-muted-foreground`。
  3.  **(M3 按钮)**：CTA 按钮 **必须**使用 `<Button size="lg">`。
  4.  **(M11 视觉)**：**必须**使用你提供的 `image_49c2ea.jpg` 作为主视觉。
  5.  **(M11 交互)**：**必须**使用 `framer-motion`（AI IDE: `pnpm add framer-motion`）为标题、副标题、按钮添加**微妙的** `fade-in-up` 动画，以提升“高级感”。

<!-- end list -->

```typescript
// AI IDE: Create this file at components/landing/HeroSection.tsx
'use client'
import { Button } from '@/components/ui/button'
import { motion } from 'framer-motion'
import Image from 'next/image'
import Link from 'next/link'
import heroImage from '@/public/images/hero-visual.jpg' // (AI IDE: 假设 `image_49c2ea.jpg` 被重命名并放入 public)

export function HeroSection({ dict }: { dict: any }) {
  return (
    <section className="container mx-auto px-4 py-24 text-center">
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5 }}
      >
        <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight leading-tight">
          {dict.title} {/* "AI 求职助手..." */}
        </h1>
        <p className="mt-6 text-lg md:text-xl text-muted-foreground max-w-3xl mx-auto">
          {dict.subtitle} {/* "上传简历与岗位 JD..." */}
        </p>
      </motion.div>
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5, delay: 0.2 }}
      >
        <Button asChild size="lg" className="mt-8">
          <Link href="/workbench">{dict.cta}</Link> {/* "开始免费试用" */}
        </Button>
      </motion.div>

      {/* M11 主视觉 */}
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5, delay: 0.4 }}
      >
        <Image
          src={heroImage} //
          alt="AI Job Assistant Visual"
          width={1200}
          height={600}
          priority // 确保首屏图片优先加载
          className="mt-12 rounded-lg shadow-2xl"
        />
      </motion.div>
    </section>
  )
}
```

---

#### 任务 3：(AI IDE) `ValueProps` 和 `HowItWorks` 节 (M11.2)

- **目标**：使用 M3 `AppCard` 和“图标列表” 来构建专业感。

- **文件**：`components/landing/ValuePropsSection.tsx`

- **AI IDE 动作**：

  1.  创建一个 3 列布局（`md:grid-cols-3`）。
  2.  将“匹配度分析”、“简历定制化”、“面试要点清单” 分别放入一个 `AppCard` 中。
  3.  **必须**在 `AppCardHeader` 中使用 `lucide-react` 图标（例如 `Target`, `FileText`, `Mic`）来增加“高级感”。

- **文件**：`components/landing/HowItWorksSection.tsx`

- **AI IDE 动作**：

  1.  创建一个 2 列布局（`md:grid-cols-2`），左侧放“信任”特性，右侧放“视觉”。
  2.  左侧**必须**使用“图标+标题+描述”的列表样式（`ul`）来展示“结构化输出”、“RAG 知识库”等特性。
  3.  右侧**必须**放置一个“伪代码”截图或 `Workbench` 的模糊截图，以增加技术可信度。

---

#### 任务 4：(AI IDE) `FAQ` 和 `CTA` 节 (M11.2)

- **目标**：解决用户疑虑，并提供“二次转化”入口。

- **文件**：`components/landing/FaqSection.tsx`

- **AI IDE 动作**：

  1.  **必须**使用 `shadcn/ui` 的 `Accordion` 组件来实现 FAQ。
  2.  将 `1.Project_Spec.md` 中的 3 个 FAQ（数据保存、移动端、付费） 作为 `AccordionItem` 填入。

- **文件**：`components/landing/CtaSection.tsx`

- **AI IDE 动作**：

  1.  创建一个**简洁的、居中的** `section`。
  2.  `h2` 标题（“准备好开始了吗？”）。
  3.  `<Button size="lg">`（同 Hero 一样的 CTA）。

---

#### 任务 5：(AI IDE) 响应式适配 (M11.3)

- **目标**：确保 M11 的所有 UI 在移动端 体验良好。
- **AI IDE 动作**：
  1.  **`HeroSection`**：确保 `h1` 字体在移动端（`text-4xl`） 不会过大（`lg:text-6xl`）。
  2.  **`ValuePropsSection`**：`md:grid-cols-3` 必须在移动端回落为 `grid-cols-1`。
  3.  **`HowItWorksSection`**：`md:grid-cols-2` 必须在移动端回落为 `grid-cols-1`（视觉在上，列表在下）。

---

#### 任务 6：(项目 Owner) 部署公域 Analytics (M11.5)

- **目标**：启用 Vercel Analytics (或 Plausible)，跟踪“公域流量”。
- **动作 (Owner)**：
  1.  登录 Vercel 仪表盘。
  2.  进入 `job-assistant` 项目，点击 `Analytics` 选项卡。
  3.  **点击“Enable”**。
  4.  (如果使用 Plausible/Umami，则 AI IDE 需要在 `app/[locale]/layout.tsx` 中添加对应的 `script` 标签)。

---

**M11 交付物：**
AI IDE（我）现在已经为你提供了 M11 里程碑的全部核心产物：

1.  **`app/[locale]/page.tsx`** (新建 - SSR/SEO 骨架)
2.  **`components/landing/*.tsx`** (新建 - 5 个核心区块组件)
3.  **M10 SEO 任务** (`generateMetadata`, `JSON-LD`) 的代码实现
4.  **M11 交互** (`framer-motion`) 和**移动端适配** 的 `tailwind` 响应式类。

**DoD (验收标准)**：

1.  `Landing Page` 在桌面端和移动端 均能完美渲染，**视觉效果**（`AppCard`, 字体, 间距）严格符合 M3 设计规范。
2.  `i18n` 切换正常，SEO 元数据（`title`） 随之变化。
3.  Vercel Analytics 仪表盘中开始出现访问数据。
