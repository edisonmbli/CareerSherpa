**TemplateCorporate** 的核心关键词是：**权威、稳重、专业**。
它主要面向金融、法务、传统行业的高管或中高级管理人才。这类简历不需要像技术岗那样强调“极客感”，但需要极强的**“公信力”**和**“结构美”**。

以下是针对 **「企业蓝调 (Corporate Blue)」** 模板的设计方案推导与示意图：

---

### 🏛️ Corporate 模板：视觉设计方案推导图

#### 1. 品牌色与调性 (Tone & Branding)

- **设计推导：** 心理学中，深蓝色（Navy Blue）代表信任、智慧与稳定性。这是全球 500 强企业的标配色。
- **落地方案：** \* **主色调：** 采用深沉的“企业蓝”（如 `#1e3a8a` 或 `#111827` 偏蓝），用于姓名、模块标题和装饰性边框。
- **辅助色：** 浅灰蓝背景（`#f8fafc`）用于标题底色或侧边点缀，增加视觉层次而不失沉稳。

#### 2. 版式架构 (Layout & Balance)

- **设计推导：** 企业简历强调“逻辑严密”和“职责清晰”。
- **落地方案：** \* **强标题栏设计：** 每一个模块标题（如 WORK EXPERIENCE）不再仅仅是带底线，而是采用“色块+文字”或“左侧粗边框”的形式，制造一种**“不可逾越的权威感”**。
- **双栏信息头部：** 头部信息不再居中，而是采用左右分栏：左侧放置姓名和 Title，右侧垂直排列联系方式，节省垂直空间的同时显得更干练。

#### 3. 字体与排版 (Typography)

- **设计推导：** 需要绝对的易读性。
- **落地方案：** \* **推荐字体：** 使用 **Roboto**（现代专业）或 **Noto Serif**（如果用户偏好传统、严谨的法务/金融感）。
- **字级阶梯：** \* **姓名：** 24pt，极简深蓝，无衬线。
- **公司名：** 与职位名同级，但通过字重（Semibold vs Regular）拉开差异，强调“平台价值”。

#### 4. 视觉装饰细节 (Signature Details)

- **设计推导：** “魔鬼在细节中”，高端简历需要细腻的纹理。
- **落地方案：** \* **衬线分隔符：** 在职位描述的 Bullet points 之间，使用极细的 0.5px 灰色水平线，帮助面试官在大段描述中快速换行。
- **精致 Icon：** 使用实心或双色调图标（Solid/Duotone），与深蓝色调统一。

---

### 🎨 Corporate 模板：高保真视觉原型示意 (Blueprint)

```text
+-------------------------------------------------------------------------------+
|  [ 姓名 / FULL NAME ] (24px, Deep Navy, Bold)             [ 头像占位 - 可选 ]  |
|  Senior Project Manager | MBA                             (Clean Box)         |
|                                                                               |
|  [📱 138-0000-0000] [📧 email@corp.com] [🔗 linkedin.com/in/name] [📍 Shanghai] |
|  (Header Divider: Subtle 1px Light Blue Line)                                 |
+-------------------------------------------------------------------------------+
|                                                                               |
|  +-------------------------------------------------------------------------+  |
|  |  WORK EXPERIENCE (14px, White Text, Deep Blue Background Pill)          |  |
|  +-------------------------------------------------------------------------+  |
|                                                                               |
|  [ 某全球五百强金融集团 ] -------------------------------------- [ 2018 - 至今 ]  |
|  (Company: 14px, Bold, Navy-800)                            (Date: 12px, Gray) |
|  [ 资深项目经理 / Senior Project Manager ]                                    |
|  (Position: 13px, Medium-Gray, Italic)                                        |
|                                                                               |
|   • 领导超过 50 人的跨国团队，成功交付价值 1.2 亿美金的数据中心迁移项目。         |
|   • 通过引入敏捷开发流程，将业务交付周期缩短了 30%。                             |
|   (Content: 13px, Line-height 1.6, Classic Bullets)                           |
|                                                                               |
|  +-------------------------------------------------------------------------+  |
|  |  CORE COMPETENCIES (Strong Left Border: 4px Navy Blue)                  |  |
|  +-------------------------------------------------------------------------+  |
|                                                                               |
|   Strategic Planning | Risk Management | Stakeholder Engagement | P&L Control |
|   (Grid Layout: Horizontal list with vertical bar separators "|" )             |
|                                                                               |
+-------------------------------------------------------------------------------+
|                                                                               |
|  [ 教育背景 / EDUCATION ]                                                      |
|  [ 某著名商学院 ] - [ 工商管理硕士 ] - [ 2015 - 2017 ]                          |
+-------------------------------------------------------------------------------+

```

---

根据我们对“企业蓝调”模板达成的**“权威感、强标题、单栏结构”**设计共识，我为你重构了 `TemplateCorporate.tsx`。

这次重构的核心提升点：

1. **精英化头部 (Executive Header)：** 采用左右分布，姓名居左强化个人品牌，联系方式居右整齐排列。
2. **强视觉锚点 (Corporate Headers)：** 模块标题采用“主色调背景块”设计，这种横向色块能瞬间锁住阅读者视线，体现职业稳重感。
3. **信息对比优化：** 公司名使用粗体加深色，职位使用中灰色，建立清晰的“平台-个人”从属逻辑。
4. **排版节律：** 增加了更细腻的间距控制，确保在信息量较大时依然保持极高的呼吸感。

```tsx
'use client'

import React from 'react'
import { TemplateProps } from './types'
import { useResumeTheme } from './hooks/useResumeTheme'
import { renderDescription, formatDate } from './utils'
import { cn } from '@/lib/utils'
import { Mail, Phone, Github, MapPin, Globe, Linkedin } from 'lucide-react'

/**
 * Corporate 模板 - 企业蓝调
 * 特点：深沉稳重、强模块化分割、权威感排版
 */
export function TemplateCorporate({
  data,
  config,
  styleConfig,
}: TemplateProps) {
  const { basics, workExperiences, projectExperiences, educations, skills } =
    data
  const theme = useResumeTheme(styleConfig)

  // 辅助组件：企业级模块标题 (带有主色调背景的横条)
  const SectionHeader = ({ title }: { title: string }) => (
    <div className="mb-4 mt-6 first:mt-0">
      <div
        className="px-3 py-1 flex items-center justify-between"
        style={{ backgroundColor: theme.themeColor }}
      >
        <h3 className="text-[13px] font-bold text-white uppercase tracking-[0.1em]">
          {title}
        </h3>
      </div>
    </div>
  )

  const sectionMap: Record<string, React.ReactNode> = {
    basics: null,
    summary: basics.summary && (
      <section style={theme.section}>
        <SectionHeader title="Professional Summary" />
        <p className="text-[13px] leading-relaxed text-gray-700 text-justify px-1">
          {basics.summary}
        </p>
      </section>
    ),
    skills: skills && (
      <section style={theme.section}>
        <SectionHeader title="Core Competencies" />
        <div className="px-1">
          {/* 企业模板倾向于逻辑清晰的列表展示而非花哨标签 */}
          <div className="grid grid-cols-1 gap-y-2 text-[13px] text-gray-700">
            {skills.split('\n').map((line, idx) => (
              <div key={idx} className="flex gap-2">
                <span className="text-gray-400 font-bold">•</span>
                <span>{line.trim()}</span>
              </div>
            ))}
          </div>
        </div>
      </section>
    ),
    workExperiences: workExperiences?.length > 0 && (
      <section style={theme.section}>
        <SectionHeader title="Work Experience" />
        <div className="space-y-6 px-1">
          {workExperiences.map((item) => (
            <div key={item.id} className="group">
              <div className="flex justify-between items-baseline mb-1">
                <h4 className="text-[15px] font-bold text-gray-900 uppercase">
                  {item.company}
                </h4>
                <span className="text-[12px] font-semibold text-gray-500 italic">
                  {formatDate(item.startDate)} —{' '}
                  {item.endDate ? formatDate(item.endDate) : 'Present'}
                </span>
              </div>
              <div className="text-[13px] font-medium text-gray-600 mb-2 flex justify-between">
                <span style={{ color: theme.themeColor }}>{item.position}</span>
                {item.location && (
                  <span className="text-gray-400 font-normal">
                    {item.location}
                  </span>
                )}
              </div>
              <div className="text-[13px] text-gray-700 leading-normal">
                {renderDescription(item.description)}
              </div>
            </div>
          ))}
        </div>
      </section>
    ),
    projectExperiences: projectExperiences?.length > 0 && (
      <section style={theme.section}>
        <SectionHeader title="Key Projects" />
        <div className="space-y-5 px-1">
          {projectExperiences.map((item) => (
            <div key={item.id}>
              <div className="flex justify-between items-baseline mb-1">
                <h4 className="text-[14px] font-bold text-gray-800">
                  {item.projectName}
                </h4>
                <span className="text-[11px] text-gray-400 font-mono">
                  {formatDate(item.startDate)} — {formatDate(item.endDate)}
                </span>
              </div>
              {item.role && (
                <div className="text-[12px] font-medium text-gray-500 mb-1">
                  {item.role}
                </div>
              )}
              <div className="text-[13px] text-gray-700">
                {renderDescription(item.description)}
              </div>
            </div>
          ))}
        </div>
      </section>
    ),
    educations: educations?.length > 0 && (
      <section style={theme.section}>
        <SectionHeader title="Education" />
        <div className="space-y-4 px-1">
          {educations.map((item) => (
            <div key={item.id} className="flex justify-between items-start">
              <div className="flex flex-col">
                <span className="text-[14px] font-bold text-gray-900">
                  {item.school}
                </span>
                <span className="text-[13px] text-gray-600">
                  {item.major} {item.degree && `| ${item.degree}`}
                </span>
              </div>
              <span className="text-[12px] text-gray-500 font-medium whitespace-nowrap">
                {formatDate(item.startDate)} — {formatDate(item.endDate)}
              </span>
            </div>
          ))}
        </div>
      </section>
    ),
  }

  return (
    <div
      className={cn(
        'bg-white w-full min-h-full transition-all duration-300 relative',
        theme.fontFamilyClass
      )}
      style={theme.container}
    >
      {/* 装饰性左边缘线 - 增加设计感 */}
      <div
        className="absolute left-0 top-0 bottom-0 w-[4px]"
        style={{ backgroundColor: theme.themeColor }}
      />

      {/* Header: Executive Split Layout */}
      <header className="mb-10 pl-2">
        <div className="flex justify-between items-start">
          <div className="flex-1">
            <h1
              className="text-[36px] font-black uppercase tracking-tight mb-1"
              style={{ color: theme.themeColor }}
            >
              {basics.name}
            </h1>
            <p className="text-[16px] font-medium text-gray-500 tracking-wide uppercase">
              Senior Professional | MBA
            </p>
          </div>

          <div
            className="flex flex-col items-end gap-1.5 pt-2 border-r-4 pr-4"
            style={{ borderColor: theme.themeColor }}
          >
            {basics.mobile && (
              <div className="flex items-center gap-2 text-[12px] text-gray-600 font-medium">
                {basics.mobile}{' '}
                <Phone size={14} style={{ color: theme.themeColor }} />
              </div>
            )}
            {basics.email && (
              <div className="flex items-center gap-2 text-[12px] text-gray-600 font-medium">
                {basics.email}{' '}
                <Mail size={14} style={{ color: theme.themeColor }} />
              </div>
            )}
            {basics.location && (
              <div className="flex items-center gap-2 text-[12px] text-gray-600 font-medium">
                {basics.location}{' '}
                <MapPin size={14} style={{ color: theme.themeColor }} />
              </div>
            )}
            <div className="flex gap-2 mt-1">
              {basics.github && <Github size={14} className="text-gray-400" />}
              {basics.linkedin && (
                <Linkedin size={14} className="text-gray-400" />
              )}
            </div>
          </div>
        </div>
      </header>

      {/* Main Content Sections */}
      <div className="flex flex-col pl-2">
        {config.order.map((key) => {
          if (config.hidden.includes(key)) return null
          return <div key={key}>{sectionMap[key]}</div>
        })}
      </div>
    </div>
  )
}
```

### 💡 重构后的设计亮点解析：

1. **侧边装饰线 (L-Border Accent)：** 代码中增加了一个 `absolute` 定位的 4px 左边缘线。这种细微的装饰能打破传统 A4 白纸的单调，同时将整个页面的视觉重心引导至左侧。
2. **SectionHeader 强色块：** 不再使用简单的 `border-b`，而是使用了 `style={{ backgroundColor: theme.themeColor }}` 的实心条。这在打印和扫描（ATS 扫描）中都能保持极高的结构清晰度。
3. **列表与间距：** 针对工作经历（Work Experience），采用了 `space-y-6` 的较大间距。这种“慷慨”的留白会给人一种工作内容非常重要、需要仔细研读的心理暗示。
4. **图标一致性：** 联系方式部分的图标采用了 `theme.themeColor` 着色，使品牌感贯穿始终。
