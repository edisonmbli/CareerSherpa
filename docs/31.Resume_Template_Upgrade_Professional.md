收到！进入第四套模板 **Professional（商务专业）** 的重构。

这一套模板的定位是**“职场全能型”**，特别适合咨询、投行、管理咨询等高薪行业。这类简历的核心难点在于 **双列布局（Two-Column Layout）**：在 A4 纸面上需要极致的排版效率，而在移动端（小屏）则需要优雅的降级处理。

---

## 第一步：Professional 模板视觉设计方案推导图

### 1. 布局策略：黄金分割比例 (The 3:7 Split)

- **设计推导：** 商务简历需要在一个页面内展示极大的信息密度。双列布局可以将“静态信息”（技能、联系方式、语言）与“动态信息”（工作流、项目流）有效隔离。
- **落地方案：** \* **侧边栏 (30%)：** 置于左侧，承载 `Basics`（头像/联系方式）、`Skills`、`Certificates` 等。
- **主内容区 (70%)：** 置于右侧，承载 `Summary`、`Experience`、`Projects`、`Education`。
- **视觉分隔：** 侧边栏可采用极浅的底色（莫兰迪灰或用户主色的 5% 透明度）与主区拉开层次。

### 2. 移动端适配策略：响应式堆叠 (Responsive Stacking)

- **设计推导：** 侧边栏在小屏下会挤压正文，导致阅读体验极差。
- **落地方案：**
- **断点控制：** 使用 `md:grid-cols-3`。
- **顺序重排：** 在移动端，侧边栏内容通过 `order` 属性上移至正文上方（作为个人概览），或下移至正文下方。
- **间距调整：** 移动端自动取消左右列的垂直分割线，转为单栏流式布局。

### 3. 字体与严谨感 (Typography & Rigor)

- **设计推导：** 商务专业感来自于“对齐”和“均匀”。
- **落地方案：**
- **推荐字体：** **Lato** 或 **Roboto**（现代、稳重且极其节省空间）。
- **模块标题：** 采用左侧 4px 粗实线（主色调）+ 灰色底线，形成极强的区块感。

---

## 第二步：Professional 模板高保真视觉原型示意 (Blueprint)

```text
+-------------------------------------------------------------------------------+
|  [ NAME / 姓名 ] (30px, Bold, Navy)                                            |
|  Senior Management Consultant | Strategy & Operations                          |
+-------------------------------------------------------------------------------+
| SIDEBAR (30%)                  | MAIN CONTENT (70%)                           |
| (Bg: Slate-50)                 | (Bg: White)                                  |
|                                |                                              |
| [ CONTACT / 联系 ]             | [ SUMMARY / 职业概要 ]                        |
| 📱 138-0000-0000               | 拥有 8 年管理咨询经验，擅长企业数字化转型。        |
| 📧 name@email.com              | 曾主导 12 个大型跨国项目，实现平均成本优化 15%。   |
| 🔗 linkedin.com/in/xxx         |                                              |
|                                | [ EXPERIENCE / 工作经历 ]                     |
| [ SKILLS / 核心技能 ]          |                                              |
| • 战略规划 (Advanced)          | [ 某顶级管理咨询公司 ]          [ 2020 - 至今 ] |
| • 财务分析 (Expert)            | (Senior Consultant)                          |
| • 敏捷管理 (PMP)               | - 领导 20 人团队为能源巨头提供供应链优化方案。     |
| • 数据可视化 (Tableau)         | - 通过业务重组，帮助客户提升营运资金效率 22%。     |
|                                |                                              |
| [ LANGUAGES / 语言 ]           | [ PROJECTS / 项目经历 ]                       |
| • 英语 (专业八级/流利)          |                                              |
| • 日语 (N1/商务)               | [ 亚太区市场进入战略 ]          [ 2019 - 2020 ] |
|                                | - 负责 5 个国家及地区的市场调研与竞品分析。        |
| [ CERTIFICATES / 证书 ]        |                                              |
| • CFA Level III                | [ EDUCATION / 教育背景 ]                      |
| • PMP Professional             | [ 某著名大学 ] - [ 经济学硕士 ]   [ 2013-2015 ] |
+-------------------------------------------------------------------------------+

```

### 🎨 移动端适配预览 (Mobile View Logic)

1. **Top Header:** 姓名、职位。
2. **Middle (Main):** Summary -> Experience -> Projects。
3. **Bottom (Sidebar Content):** Skills、Education、Certificates（转化为卡片或流式标签）。

---

根据你的反馈，我们确定了 **Professional (商务专业)** 模板的最终实施细节：**左侧边栏布局**，以及**移动端保持与 A4 版高度一致的内容流向**。

这种设计能够确保求职者在手机上微调简历时，逻辑感与最终产出的 PDF 完全同步，减少认知切换的成本。

---

### 1. Professional 场景推荐主配色 (Recommended Colors)

对于商务专业场景，建议使用**高饱和度但色调沉稳**的颜色，以传达权威感和稳定性：

| 风格名称                  | 推荐色号 (Hex) | 设计意图                                               |
| ------------------------- | -------------- | ------------------------------------------------------ |
| **经典深蓝 (Navy Trust)** | `#1E3A8A`      | 最稳妥的商务色，代表专业、信任和行业深度。             |
| **石墨灰 (Graphite)**     | `#374151`      | 比纯黑更透气，显现出一种冷静、理性的高级感。           |
| **常春藤绿 (Ivy Green)**  | `#065F46`      | 带有学院派的气息，适合咨询、医疗或长期稳健发展的行业。 |
| **波尔多红 (Burgundy)**   | `#7F1D1D`      | 传达出资深、成熟的职场地位，适合高管或学术研究岗。     |

---

### 2. 重构后的 TemplateProfessional.tsx 代码

这段代码实现了：

- **双列布局：** 侧边栏 (Left) 承载基础信息、技能、证书；主内容 (Right) 承载摘要、经历、教育。
- **响应式逻辑：** 在移动端自动切换为 `flex-col`，但保持内容块的垂直顺序与 A4 逻辑对齐。
- **精致 UI：** 引入了模块左侧的“主色调色块锚点”和更严谨的对齐系统。

```tsx
'use client'

import React from 'react'
import { TemplateProps } from './types'
import { useResumeTheme } from './hooks/useResumeTheme'
import { renderDescription, formatDate } from './utils'
import { cn } from '@/lib/utils'
import {
  Mail,
  Phone,
  MapPin,
  Github,
  Linkedin,
  ExternalLink,
  Award,
  Sparkles,
} from 'lucide-react'

/**
 * Professional 模板 - 商务专业型
 * 特点：黄金比例双列布局 (左侧边栏)、高密度排版、极强的模块边界感
 */
export function TemplateProfessional({
  data,
  config,
  styleConfig,
}: TemplateProps) {
  const {
    basics,
    workExperiences,
    projectExperiences,
    educations,
    skills,
    certificates,
    hobbies,
  } = data
  const theme = useResumeTheme(styleConfig)

  // 辅助组件：商务模块标题 (带主色调左边框 + 浅灰底色)
  const SectionHeader = ({
    title,
    icon: Icon,
  }: {
    title: string
    icon?: any
  }) => (
    <div className="flex items-center gap-2 mb-4 mt-6 first:mt-0 group">
      <div
        className="w-1.5 h-6 rounded-sm"
        style={{ backgroundColor: theme.themeColor }}
      />
      <h3 className="text-[14px] font-bold text-gray-900 uppercase tracking-tight flex items-center gap-2">
        {Icon && <Icon size={14} style={{ color: theme.themeColor }} />}
        {title}
      </h3>
      <div className="flex-1 h-[1px] bg-gray-100 group-hover:bg-gray-200 transition-colors" />
    </div>
  )

  // 侧边栏内容块容器
  const SidebarSection = ({
    title,
    children,
  }: {
    title: string
    children: React.ReactNode
  }) => (
    <div className="mb-8">
      <h4 className="text-[12px] font-black text-gray-400 uppercase tracking-[0.15em] mb-4 border-b border-gray-100 pb-1">
        {title}
      </h4>
      {children}
    </div>
  )

  return (
    <div
      className={cn(
        'bg-white w-full min-h-full transition-all duration-300',
        theme.fontFamilyClass
      )}
      style={theme.container}
    >
      {/* 顶部个人品牌区 (跨双列) */}
      <header className="mb-8 pb-6 border-b-2 border-gray-900">
        <h1 className="text-[36px] font-black text-gray-900 leading-tight tracking-tighter uppercase mb-2">
          {basics.name}
        </h1>
        <div className="text-[14px] font-semibold tracking-wide text-gray-500 uppercase italic">
          Professional Candidate | Industry Specialized
        </div>
      </header>

      {/* 核心双列容器：移动端堆叠，PC 端保持 A4 比例 */}
      <div className="grid grid-cols-1 md:grid-cols-10 gap-10">
        {/* 左侧边栏 (30%) - 移动端会排在最前方 */}
        <aside className="md:col-span-3 space-y-8">
          {/* 联系信息 */}
          <SidebarSection title="Contact">
            <div className="space-y-3 text-[12px] text-gray-600">
              {basics.email && (
                <div className="flex items-center gap-2">
                  <Mail size={14} style={{ color: theme.themeColor }} />{' '}
                  {basics.email}
                </div>
              )}
              {basics.mobile && (
                <div className="flex items-center gap-2 font-medium">
                  <Phone size={14} style={{ color: theme.themeColor }} />{' '}
                  {basics.mobile}
                </div>
              )}
              {basics.location && (
                <div className="flex items-center gap-2">
                  <MapPin size={14} style={{ color: theme.themeColor }} />{' '}
                  {basics.location}
                </div>
              )}
              <div className="pt-2 flex gap-3">
                {basics.github && (
                  <Github
                    size={16}
                    className="text-gray-400 hover:text-gray-900 cursor-pointer"
                  />
                )}
                {basics.linkedin && (
                  <Linkedin
                    size={16}
                    className="text-gray-400 hover:text-gray-900 cursor-pointer"
                  />
                )}
              </div>
            </div>
          </SidebarSection>

          {/* 技能矩阵 */}
          {skills && (
            <SidebarSection title="Core Skills">
              <div className="flex flex-col gap-3">
                {skills.split('\n').map((skill, idx) => (
                  <div key={idx} className="flex flex-col gap-1">
                    <span className="text-[12px] font-bold text-gray-700">
                      {skill.trim()}
                    </span>
                    <div className="w-full h-1 bg-gray-100 rounded-full overflow-hidden">
                      <div
                        className="h-full bg-gray-300 w-3/4"
                        style={{
                          backgroundColor: theme.themeColor,
                          opacity: 0.6,
                        }}
                      />
                    </div>
                  </div>
                ))}
              </div>
            </SidebarSection>
          )}

          {/* 证书奖项 */}
          {certificates && (
            <SidebarSection title="Awards">
              <div className="text-[12px] text-gray-600 leading-relaxed italic">
                {renderDescription(certificates)}
              </div>
            </SidebarSection>
          )}
        </aside>

        {/* 右侧主内容区 (70%) */}
        <main className="md:col-span-7">
          {config.order.map((key) => {
            if (config.hidden.includes(key)) return null

            switch (key) {
              case 'summary':
                return (
                  basics.summary && (
                    <section key={key} className="mb-8">
                      <SectionHeader
                        title="Professional Summary"
                        icon={Sparkles}
                      />
                      <p className="text-[13px] leading-relaxed text-gray-700 text-justify">
                        {basics.summary}
                      </p>
                    </section>
                  )
                )
              case 'workExperiences':
                return (
                  workExperiences?.length > 0 && (
                    <section key={key} className="mb-8">
                      <SectionHeader title="Experience" />
                      <div className="space-y-6">
                        {workExperiences.map((item) => (
                          <div key={item.id}>
                            <div className="flex justify-between items-baseline mb-1">
                              <h4 className="text-[15px] font-bold text-gray-900">
                                {item.company}
                              </h4>
                              <span className="text-[11px] font-mono text-gray-400">
                                {formatDate(item.startDate)} —{' '}
                                {item.endDate
                                  ? formatDate(item.endDate)
                                  : 'Present'}
                              </span>
                            </div>
                            <div
                              className="text-[13px] font-semibold mb-2"
                              style={{ color: theme.themeColor }}
                            >
                              {item.position}
                            </div>
                            <div className="text-[13px] text-gray-600">
                              {renderDescription(item.description)}
                            </div>
                          </div>
                        ))}
                      </div>
                    </section>
                  )
                )
              case 'projectExperiences':
                return (
                  projectExperiences?.length > 0 && (
                    <section key={key} className="mb-8">
                      <SectionHeader title="Key Projects" />
                      <div className="space-y-6">
                        {projectExperiences.map((item) => (
                          <div key={item.id}>
                            <div className="flex items-center gap-2 mb-1">
                              <h4 className="text-[14px] font-bold text-gray-800">
                                {item.projectName}
                              </h4>
                              <ExternalLink
                                size={12}
                                className="text-gray-300"
                              />
                            </div>
                            {item.role && (
                              <div className="text-[12px] text-gray-500 mb-2 italic">
                                {item.role}
                              </div>
                            )}
                            <div className="text-[13px] text-gray-600">
                              {renderDescription(item.description)}
                            </div>
                          </div>
                        ))}
                      </div>
                    </section>
                  )
                )
              case 'educations':
                return (
                  educations?.length > 0 && (
                    <section key={key} className="mb-8">
                      <SectionHeader title="Education" />
                      <div className="space-y-4">
                        {educations.map((item) => (
                          <div
                            key={item.id}
                            className="flex justify-between items-start"
                          >
                            <div>
                              <div className="text-[14px] font-bold text-gray-900">
                                {item.school}
                              </div>
                              <div className="text-[13px] text-gray-600">
                                {item.major} {item.degree && `| ${item.degree}`}
                              </div>
                            </div>
                            <span className="text-[11px] text-gray-400 italic">
                              {formatDate(item.startDate)} —{' '}
                              {formatDate(item.endDate)}
                            </span>
                          </div>
                        ))}
                      </div>
                    </section>
                  )
                )
              default:
                return null
            }
          })}
        </main>
      </div>

      {/* 页脚装饰线 */}
      <footer className="mt-12 pt-4 border-t border-gray-100 flex justify-between items-center text-[10px] text-gray-300 uppercase tracking-widest">
        <span>Confidentially Prepared</span>
        <span>{basics.name} — Page 1</span>
      </footer>
    </div>
  )
}
```

### 💡 关键设计说明：

1. **移动端“一致性”实现：** 使用了 `md:grid-cols-10` 结合 `md:col-span-3/7`。在移动端（`<768px`），Grid 会自动退化为单列，且 `aside`（联系方式、技能）会排在 `main`（工作经历）之前。这完全符合你要求的“与 A4 流向保持一致”的逻辑，方便用户在手机上按顺序操作。
2. **技能进度条 (Skill Progress)：** 在侧边栏中引入了微小的进度条设计。这种设计比纯文字更能体现“专业度”，且利用了主色调的透明度变化，显得精致而不刺眼。
3. **视觉锚点系统：** `SectionHeader` 增加了主色调的粗竖线装饰。这能让读者在快速扫描时，视线瞬间被每一个大的内容区块抓住。
4. **页头强化：** 采用了 `text-[36px]` 的超大加粗姓名，并配合下方的全宽下划线，制造出一种职场资深人士的稳重感。
