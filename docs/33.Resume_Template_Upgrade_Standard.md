**Standard（标准通用型）** 模板的特殊地位：它是简历系统中的“最后一道防线”，目标是 **100% 的容错率**。

---

### 🔎 当前 Standard 模板的 UI/UX 点评

#### 1. 看到的问题 (Pain Points)

- **视觉层级过于扁平 (Flat Hierarchy)：** 目前姓名很大，但下方的联系方式、模块标题、工作职位的字号和字重过于接近。这导致读者的视线没有明显的“钩子”，容易产生阅读疲劳。
- **空间利用率较低 (Space Efficiency)：** 头部（Header）占据了过多的垂直空间，但信息密度却不高。对于工作经验丰富的职场人，这可能导致简历被迫进入第二页。
- **缺乏“专业修饰”感：** 虽然它是兜底模板，但目前看起来更像是一个结构化的文本文件，而不是一份经过设计的职场文件。例如，模块间的分割线（`border-b`）过于生硬，缺乏呼吸感。
- **Icon 与文字的比例：** 现有的图标（如 Phone, Mail）在标准布局下略显突兀，没有很好地融入文本流。

#### 2. 建议优化空间 (Optimization Space)

- **重塑“信笺头” (The Letterhead Header)：** 将头部信息重新编排，采用更紧凑的多列或网格布局，使其看起来像正式的商业信函头部。
- **引入“节奏感”间距：** 标准模板不代表等距。我们需要在“公司名-职位”之间用窄间距，在“模块-模块”之间用宽间距，形成视觉上的逻辑归类。
- **极致的文字细节 (Micro-Typography)：**
- **日期处理：** 建议将日期设为更轻的字重（Light）或弱灰色，使其不干扰职位的阅读，但在寻找时清晰可见。
- **对齐逻辑：** 严格对齐左侧边缘，确保扫描时的流畅性。

- **ATS 友好型的“隐形装饰”：** 在不破坏机器读取的前提下，加入极细的分割线或主色调的微小点缀（如 Section 标题左侧的一个小方块）。

---

### 🏛️ Standard 模板：视觉设计方案推导图

针对 Standard 模板，我提出的升级策略是：**「克制的专业感 (Restrained Professionalism)」**。

#### 1. 视觉设计方案推导图

- **版式骨架：** 严格的单栏流式。
- **主色调应用：** 仅出现在 **模块标题文字** 和 **关键链接** 上，其余保持纯黑灰，确保打印时的高度清晰。
- **字体建议：** 默认使用 **Inter** 或 **Roboto**（无衬线），若求职者偏向学术或高端管理，可一键切换为 **Noto Serif**。
- **移动端适配：** 由于是单栏，移动端只需微调内边距即可，具有天然的跨端一致性。

#### 2. 高保真视觉原型示意 (Blueprint)

```text
+-------------------------------------------------------------------------------+
|                                                                               |
|  [ 姓名 / FULL NAME ] (32px, Black, Semibold)                                  |
|  ---------------------------------------------------------------------------  |
|  📧 email@example.com  |  📱 138-0000-0000  |  📍 Beijing  |  🔗 LinkedIn/GitHub   |
|  (Contact Row: 12px, Gray-500, Centered or Left-aligned with subtle dividers) |
|                                                                               |
|  [ 职业概要 / SUMMARY ]                                                        |
|  (Section Header: 14px, Bold, Primary Color, with 1px border-bottom)          |
|  8年大厂前端开发经验，主导过3个千万级用户项目...                                |
|                                                                               |
|  [ 工作经历 / WORK EXPERIENCE ]                                                |
|                                                                               |
|  [ 某互联网公司 ]                                          [ 2020.01 - 至今 ]  |
|  (Company: 14px, Bold, Gray-900)                          (Date: 12px, Muted) |
|  [ 高级前端工程师 ]                                                           |
|  (Position: 13px, Medium, Gray-700, Italic)                                   |
|                                                                               |
|  • 负责... (Bullet points: 13px, Leading-relaxed, gray-600)                    |
|  • 优化...                                                                    |
|                                                                               |
|  [ 教育背景 / EDUCATION ]                                                      |
|  [ 某重点大学 ] · [ 计算机科学 ] · [ 本科 ]                  [ 2012.09 - 2016.07 ] |
|                                                                               |
+-------------------------------------------------------------------------------+

```

---

对于 **Standard（标准通用型）** 模板，我们的目标是打造一个“永不出错”且“充满专业细节”的版式。

### 1. 推荐主色调：安全且专业 (Safe & Professional Palette)

标准模板的配色应以“沉稳”为主，避免过于跳跃。以下是三组建议色号，适合 95% 以上的投递场景：

| 风格名称                       | 建议色号 (Hex) | 适用场景                                                                   |
| ------------------------------ | -------------- | -------------------------------------------------------------------------- |
| **职场蓝 (Professional Navy)** | `#1E40AF`      | **最通用的选择**。适用于金融、咨询、法律及传统大厂，代表严谨与信任。       |
| **石墨黑 (Classic Charcoal)**  | `#374151`      | **极致克制**。适合所有行业，比纯黑更柔和，阅读体验最舒适。                 |
| **深灰青 (Safe Slate)**        | `#334155`      | **现代商务**。在传统中带有一丝科技感，适合互联网运营、产品及通用职能岗位。 |

---

### 2. 重构后的 TemplateStandard.tsx 代码落地

此次重构实现了：

1. **仪式感信笺头部：** 姓名居中并放大，联系方式通过分隔符横向排开，极大地节省了空间。
2. **“短粗线”模块标识：** 模块标题采用 `inline-block` 配合下方 3px 厚的主色调短线，视觉焦点极度明确。
3. **极致的垂直节律：** 严格遵循 8px 间距规范，让原本单调的单栏布局产生节奏感。

```tsx
'use client'

import React from 'react'
import { TemplateProps } from './types'
import { useResumeTheme } from './hooks/useResumeTheme'
import { renderDescription, formatDate } from './utils'
import { cn } from '@/lib/utils'
import { Mail, Phone, MapPin, Github, Linkedin, Globe } from 'lucide-react'

/**
 * Standard 模板 - 标准通用型
 * 特点：姓名居中平衡、信笺式头部、克制着色、短粗线视觉锚点
 */
export function TemplateStandard({ data, config, styleConfig }: TemplateProps) {
  const {
    basics,
    workExperiences,
    projectExperiences,
    educations,
    skills,
    certificates,
  } = data
  const theme = useResumeTheme(styleConfig)

  // 辅助组件：标准模块标题 (文字下方短粗线)
  const SectionHeader = ({ title }: { title: string }) => (
    <div className="mb-4 mt-8 first:mt-0">
      <div className="inline-block">
        <h3 className="text-[14px] font-bold text-gray-900 uppercase tracking-wider mb-1">
          {title}
        </h3>
        <div
          className="h-[3px] w-8 rounded-full"
          style={{ backgroundColor: theme.themeColor }}
        />
      </div>
    </div>
  )

  const sectionMap: Record<string, React.ReactNode> = {
    basics: null,
    summary: basics.summary && (
      <section style={theme.section}>
        <SectionHeader title="Professional Summary" />
        <p className="text-[13px] leading-relaxed text-gray-700 text-justify">
          {basics.summary}
        </p>
      </section>
    ),
    skills: skills && (
      <section style={theme.section}>
        <SectionHeader title="Skills & Interests" />
        <div className="text-[13px] text-gray-700 leading-relaxed px-1">
          {/* 将技能以平铺方式展示，节省空间且整洁 */}
          {skills.split('\n').map((skill, i) => (
            <span key={i} className="inline-block mr-4 mb-1">
              <span className="text-gray-300 mr-2">•</span>
              {skill.trim()}
            </span>
          ))}
        </div>
      </section>
    ),
    workExperiences: workExperiences?.length > 0 && (
      <section style={theme.section}>
        <SectionHeader title="Work Experience" />
        <div className="space-y-6">
          {workExperiences.map((item) => (
            <div key={item.id}>
              <div className="flex justify-between items-baseline mb-1">
                <h4 className="text-[15px] font-bold text-gray-900">
                  {item.company}
                </h4>
                <span className="text-[12px] font-medium text-gray-500 tabular-nums">
                  {formatDate(item.startDate)} —{' '}
                  {item.endDate ? formatDate(item.endDate) : 'Present'}
                </span>
              </div>
              <div className="flex justify-between items-baseline mb-2">
                <span className="text-[13px] font-semibold italic text-gray-600">
                  {item.position}
                </span>
                {item.location && (
                  <span className="text-[11px] text-gray-400 uppercase">
                    {item.location}
                  </span>
                )}
              </div>
              <div className="text-[13px] text-gray-700 leading-normal">
                {renderDescription(item.description)}
              </div>
            </div>
          ))}
        </div>
      </section>
    ),
    projectExperiences: projectExperiences?.length > 0 && (
      <section style={theme.section}>
        <SectionHeader title="Projects" />
        <div className="space-y-5">
          {projectExperiences.map((item) => (
            <div key={item.id}>
              <div className="flex justify-between items-baseline mb-1">
                <h4 className="text-[14px] font-bold text-gray-800">
                  {item.projectName}
                </h4>
                <span className="text-[11px] text-gray-400 italic">
                  {formatDate(item.startDate)} — {formatDate(item.endDate)}
                </span>
              </div>
              {item.role && (
                <div
                  className="text-[12px] font-medium mb-2"
                  style={{ color: theme.themeColor }}
                >
                  {item.role}
                </div>
              )}
              <div className="text-[13px] text-gray-700">
                {renderDescription(item.description)}
              </div>
            </div>
          ))}
        </div>
      </section>
    ),
    educations: educations?.length > 0 && (
      <section style={theme.section}>
        <SectionHeader title="Education" />
        <div className="space-y-4">
          {educations.map((item) => (
            <div key={item.id} className="flex justify-between items-baseline">
              <div className="flex items-baseline gap-3">
                <span className="text-[14px] font-bold text-gray-900">
                  {item.school}
                </span>
                <span className="text-[13px] text-gray-600">
                  {item.major} {item.degree && `| ${item.degree}`}
                </span>
              </div>
              <span className="text-[12px] text-gray-500 tabular-nums">
                {formatDate(item.startDate)} — {formatDate(item.endDate)}
              </span>
            </div>
          ))}
        </div>
      </section>
    ),
  }

  return (
    <div
      className={cn(
        'bg-white w-full min-h-full transition-all duration-300',
        theme.fontFamilyClass
      )}
      style={theme.container}
    >
      {/* Header: Centered Identity & Horizontal Contact */}
      <header className="mb-10 text-center">
        <h1 className="text-[32px] font-bold text-gray-900 tracking-tighter mb-4">
          {basics.name}
        </h1>

        <div className="flex flex-wrap justify-center items-center gap-x-4 gap-y-2 text-[12px] text-gray-500">
          {basics.mobile && (
            <div className="flex items-center gap-1">
              <Phone size={12} style={{ color: theme.themeColor }} />{' '}
              {basics.mobile}
            </div>
          )}
          {basics.email && (
            <div className="flex items-center gap-1">
              <Mail size={12} style={{ color: theme.themeColor }} />{' '}
              {basics.email}
            </div>
          )}
          {basics.location && (
            <div className="flex items-center gap-1">
              <MapPin size={12} style={{ color: theme.themeColor }} />{' '}
              {basics.location}
            </div>
          )}
          {basics.github && (
            <div className="flex items-center gap-1 underline underline-offset-2">
              <Github size={12} /> {basics.github}
            </div>
          )}
          {basics.linkedin && (
            <div className="flex items-center gap-1 underline underline-offset-2">
              <Linkedin size={12} /> LinkedIn
            </div>
          )}
        </div>
      </header>

      {/* Main Sections Body */}
      <div className="flex flex-col">
        {config.order.map((key) => {
          if (config.hidden.includes(key)) return null
          return <div key={key}>{sectionMap[key]}</div>
        })}
      </div>

      {/* Footer Placeholder for Printing */}
      <div className="mt-16 text-center border-t border-gray-50 pt-4">
        <p className="text-[10px] text-gray-200 uppercase tracking-widest">
          Standard Professional Template • CareerSherpa AI
        </p>
      </div>
    </div>
  )
}
```

### 💡 标准模板的精细化亮点：

1. **姓名与间距的平衡：** 姓名使用了 `tracking-tighter`，使其在 32px 的大字号下依然显得紧致有力。
2. **横向联系区：** 通过 `flex-wrap` 和 `gap-x-4` 实现了联系方式的自动排列。这种布局在 A4 纸顶部显得非常整洁，比垂直堆叠更具商业美感。
3. **短粗线的实现：** 我们使用了一个 `inline-block` 的容器来包裹标题，并在其下方放置了一个 3px 高、8px 宽的 `div`。这个微小的装饰是目前很多高级付费简历模板的标配，能显著提升模板的“设计感”。
4. **信息的归并：** 技能部分采用了“点号分隔”的平铺展示，这对于标准简历来说是最高效的排版方式，能有效压缩简历长度。

---
