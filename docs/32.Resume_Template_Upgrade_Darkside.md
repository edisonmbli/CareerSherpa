在对 **TemplateDarkSidebar（深色侧边）** 进行重构之前，我们需要先从 UI/UX 设计维度定好它的“性格”与“场景”。

---

### 🧠 设计思索：Dark Sidebar 的灵魂

#### 1. 适合什么样的岗位和场景？

- **岗位性质：** 最适合**创意类、初创科技、个人品牌导向**的岗位。例如：UI/UX 设计师、产品经理、前端开发、数字营销经理。
- **求职场景：** 目标公司注重创新、个性和视觉表现力。它能让求职者在成百上千份白纸黑字的简历中脱颖而出，像一份精心设计的“个人品牌说明书”。
- **心理暗示：** 深色侧边传递出“自信”、“专业且有设计感”以及“对细节的高度掌控”。

#### 2. 需要哪些针对性的细节设计？

- **视觉重量平衡：** 深色区域视觉上非常“重”。如果右侧内容太散，整个版面会失衡。因此，右侧内容需要强有力的标题和整齐的边距来锚定视觉。
- **深色区的可读性：** 在深色背景上，文字需要适度增加**字间距（Letter Spacing）和对比度**，避免因光晕效应导致阅读疲劳。
- **色彩连贯性：** 侧边栏不应只是简单的黑色。它应该是**主色调的超深版本（Deep Shade）**，与右侧的主色调点缀形成呼应。

---

### 第一步：「视觉设计方案推导图」

#### 1. 结构布局：不对称美学 (The 35/65 Split)

- **侧边栏 (35%)：** 采用深色背景。作为“快速索引区”，放置头像、联系方式、核心技能、语言能力。
- **内容区 (65%)：** 纯白背景。作为“深度阅读区”，承载工作经历、项目详情、教育背景。

#### 2. 字体与排版：现代极简 (Modern Sans)

- **侧边栏文字：** 采用白色 (#FFFFFF) 配合透明度。关键信息用 100% 不透明度，次要信息（如 Icon 旁的文字）用 80%。
- **内容区文字：** 采用深炭灰 (#1F2937)，确保在长文阅读下的舒适度。
- **层级对比：** 利用加粗的无衬线体（如 **Inter** 或 **Roboto**）来强化模块入口。

#### 3. 装饰元素：光感与维度 (Glow & Dimension)

- **技能视觉化：** 在深色背景下，进度条或标签应带有“发光感”，使用主色调的高亮版本。
- **转场逻辑：** 侧边栏与内容区的交界处可以不加实线，而是通过阴影（Subtle Shadow）或纯色的色块对比产生自然的边界。

---

### 第二步：Dark Sidebar 模板高保真视觉原型示意 (Blueprint)

```text
+-------------------------------------------------------------------------------+
|  SIDEBAR (35% Width)           |  MAIN CONTENT (65% Width)                    |
|  (Background: Primary-900)     |  (Background: White)                         |
|                                |                                              |
|  [ 👤 头像/Photo ]             |  [ 姓名 / NAME ] (32px, Bold, Gray-900)      |
|  (Circle, 120px, White Border) |  [ 职位 / TITLE ] (16px, Primary Color)      |
|                                |                                              |
|  [ CONTACT / 联系方式 ]         |  [ SUMMARY / 职业概要 ]                      |
|  📧 email@example.com          |  资深 UI 设计师，拥有 5 年数字化产品开发经验。|
|  📱 138-0000-0000              |  专注于通过用户研究和交互设计提升产品转化率。  |
|  📍 Beijing, China             |                                              |
|  (White text with 80% opacity) |  [ WORK EXPERIENCE / 工作经历 ]              |
|                                |                                              |
|  [ SKILLS / 技能矩阵 ]         |  [ 某头部互联网公司 ]          [ 2021 - 至今 ] |
|  UI Design  [||||||||--]       |  (Senior Product Designer)                   |
|  Figma      [||||||||||]       |  - 负责主站 App 的 4.0 版本全面改版设计。       |
|  React      [||||||----]       |  - 建立并维护了全公司的 Design System。      |
|                                |                                              |
|  [ LANGUAGE / 语言 ]           |  [ PROJECTS / 项目经历 ]                      |
|  • English (Professional)      |                                              |
|  • Mandarin (Native)           |  [ Careersherpa AI 助手 ]     [ 个人项目 ]    |
|                                |  - 独立完成从 0 到 1 的移动端交互原型。        |
|  [ INTERESTS / 爱好 ]          |                                              |
|  Photography | Travel | Tech   |  [ EDUCATION / 教育背景 ]                     |
|                                |  [ 某美术学院 ] - [ 视觉传达 ]   [ 2013-2017 ] |
+-------------------------------------------------------------------------------+

```

针对 **Dark Sidebar（深色侧边）** 模板的深度反馈，我从 UI/UX 设计师的角度为你提供最终的逻辑整合方案，并完成代码落地。

### 1. 设计疑问解答与方案优化

- **姓名信息的摆放：**
- **设计师建议：放到左侧头像下方。** \* **理由：** 既然没有 Title 信息，姓名如果孤零零放在右侧上方，会显得正文区“头重脚轻”且视觉重心涣散。将姓名移至深色侧边栏的头像下方，可以与头像共同构成一个完整的 **“个人品牌识别区 (Identity Card)”**。深色背景能极好地衬托出白色的姓名文字，产生强烈的视觉冲击力。

- **教育背景的摆放：**
- **设计师建议：移至左侧边栏（Contact 下方）。**
- **理由：** 这是一个非常明智的改动。侧边栏通常承载“静态/资质类”信息，而正文承载“动态/成果类”信息。将教育经历移至左侧，不仅能平衡左右密度，还能让右侧正文区腾出更多空间给最重要的“工作经历”和“项目经历”，提升阅读者的扫视效率。

---

### 2. 推荐配置建议

- **推荐文字调性：**
- 侧边栏标签（Skills, Education）：使用 **全大写 (Uppercase)** + **加宽字间距 (Tracking-widest)**，增加精致感。
- 姓名：使用 **Bold/Black 字重**，增加分量感。

- **推荐主配色：**
- **极客深青 (Cyber Teal):** `#0D9488`（侧边栏将呈现为深沉的墨青色）。
- **星际蓝 (Interstellar Blue):** `#2563EB`（侧边栏将呈现为深邃的午夜蓝）。
- **灰烬黑 (Volcanic Ash):** `#4B5563`（侧边栏将呈现为高级的深碳灰）。

---

### 3. 重构后的 TemplateDarkSidebar.tsx 代码落地

代码实现了：

1. **侧边栏超深色处理：** 通过 CSS `brightness(0.15)` 滤镜自动计算 `themeColor` 的“超深版”，确保色调统一。
2. **移动端自适应逻辑：** 在小屏下，头像与姓名居中置顶，随后是侧边信息，最后是详细经历。
3. **信息重排：** 姓名与教育经历已按建议移至侧边栏。

```tsx
'use client'

import React from 'react'
import { TemplateProps } from './types'
import { useResumeTheme } from './hooks/useResumeTheme'
import { renderDescription, formatDate } from './utils'
import { cn } from '@/lib/utils'
import {
  Mail,
  Phone,
  MapPin,
  Github,
  Linkedin,
  GraduationCap,
  User,
  Trophy,
} from 'lucide-react'

export function TemplateDarkSidebar({
  data,
  config,
  styleConfig,
}: TemplateProps) {
  const {
    basics,
    workExperiences,
    projectExperiences,
    educations,
    skills,
    certificates,
  } = data
  const theme = useResumeTheme(styleConfig)

  // 辅助组件：侧边栏小标题
  const SidebarTitle = ({
    title,
    icon: Icon,
  }: {
    title: string
    icon?: any
  }) => (
    <div className="mb-4 mt-8 first:mt-0 flex items-center gap-2 border-b border-white/10 pb-2">
      {Icon && <Icon size={14} className="text-white/40" />}
      <h4 className="text-[11px] font-black text-white/50 uppercase tracking-[0.2em]">
        {title}
      </h4>
    </div>
  )

  // 辅助组件：主内容区标题
  const MainTitle = ({ title }: { title: string }) => (
    <div className="mb-4 mt-10 first:mt-0 relative">
      <h3 className="text-[15px] font-bold text-gray-900 uppercase tracking-tight relative z-10 bg-white pr-4 inline-block">
        {title}
      </h3>
      <div className="absolute top-1/2 left-0 w-full h-[1px] bg-gray-100 -z-0" />
      <div
        className="mt-1 h-[3px] w-8 rounded-full"
        style={{ backgroundColor: theme.themeColor }}
      />
    </div>
  )

  return (
    <div
      className={cn(
        'bg-white w-full min-h-full flex flex-col md:flex-row transition-all duration-300',
        theme.fontFamilyClass
      )}
    >
      {/* 1. 侧边栏 (Dark Sidebar) - 占 35% */}
      <aside
        className="w-full md:w-[32%] p-8 flex flex-col items-center md:items-start text-white relative overflow-hidden"
        style={{
          // 巧妙地通过滤镜生成“超深版”背景
          backgroundColor: theme.themeColor,
          filter: 'brightness(0.15) saturate(1.2)',
        }}
      >
        {/* 背景装饰纹理 */}
        <div className="absolute top-0 right-0 w-32 h-32 bg-white/5 rounded-full -translate-y-16 translate-x-16" />

        {/* 头像与姓名区 (Identity Card) */}
        <div className="flex flex-col items-center md:items-start mb-10 w-full">
          <div className="w-24 h-24 rounded-2xl bg-white/10 border border-white/20 flex items-center justify-center mb-6 overflow-hidden shadow-2xl">
            <User size={48} className="text-white/20" />
          </div>
          <h1 className="text-[28px] font-black tracking-tighter leading-none mb-2">
            {basics.name}
          </h1>
          <div
            className="h-1 w-12 rounded-full"
            style={{ backgroundColor: theme.themeColor }}
          />
        </div>

        {/* 联系方式 */}
        <div className="w-full space-y-4">
          <SidebarTitle title="Contact" />
          <div className="space-y-3 text-[12px] text-white/70 font-light">
            {basics.email && (
              <div className="flex items-center gap-3">
                <Mail size={14} className="text-white/40" /> {basics.email}
              </div>
            )}
            {basics.mobile && (
              <div className="flex items-center gap-3">
                <Phone size={14} className="text-white/40" /> {basics.mobile}
              </div>
            )}
            {basics.location && (
              <div className="flex items-center gap-3">
                <MapPin size={14} className="text-white/40" /> {basics.location}
              </div>
            )}
          </div>
        </div>

        {/* 教育背景 (已移至侧边栏) */}
        {educations?.length > 0 && (
          <div className="w-full mt-10">
            <SidebarTitle title="Education" icon={GraduationCap} />
            <div className="space-y-6">
              {educations.map((edu) => (
                <div key={edu.id} className="group">
                  <div className="text-[13px] font-bold text-white mb-1">
                    {edu.school}
                  </div>
                  <div className="text-[11px] text-white/50">{edu.major}</div>
                  <div className="text-[10px] text-white/30 mt-1 tabular-nums italic">
                    {formatDate(edu.startDate)} — {formatDate(edu.endDate)}
                  </div>
                </div>
              ))}
            </div>
          </div>
        )}

        {/* 核心技能 */}
        {skills && (
          <div className="w-full mt-10">
            <SidebarTitle title="Key Skills" />
            <div className="flex flex-wrap gap-2">
              {skills.split('\n').map((skill, i) => (
                <span
                  key={i}
                  className="px-2 py-1 bg-white/5 border border-white/10 rounded-md text-[11px] text-white/80"
                >
                  {skill.trim()}
                </span>
              ))}
            </div>
          </div>
        )}
      </aside>

      {/* 2. 主内容区 (Main Content) - 占 65% */}
      <main className="flex-1 p-10 bg-white">
        {config.order.map((key) => {
          if (config.hidden.includes(key) || key === 'educations') return null

          switch (key) {
            case 'summary':
              return (
                basics.summary && (
                  <section key={key} className="mb-10">
                    <MainTitle title="Professional Summary" />
                    <p className="text-[13px] leading-relaxed text-gray-600 text-justify">
                      {basics.summary}
                    </p>
                  </section>
                )
              )
            case 'workExperiences':
              return (
                workExperiences?.length > 0 && (
                  <section key={key} className="mb-10">
                    <MainTitle title="Work Experience" />
                    <div className="space-y-8">
                      {workExperiences.map((item) => (
                        <div
                          key={item.id}
                          className="relative pl-6 border-l border-gray-100"
                        >
                          <div
                            className="absolute -left-[5px] top-1 w-[9px] h-[9px] rounded-full"
                            style={{ backgroundColor: theme.themeColor }}
                          />
                          <div className="flex justify-between items-start mb-2">
                            <h4 className="text-[15px] font-bold text-gray-900">
                              {item.company}
                            </h4>
                            <span className="text-[11px] font-mono text-gray-400">
                              {formatDate(item.startDate)} —{' '}
                              {formatDate(item.endDate) || 'Present'}
                            </span>
                          </div>
                          <div className="text-[13px] font-semibold text-gray-500 mb-2 italic">
                            {item.position}
                          </div>
                          <div className="text-[13px] text-gray-600 leading-normal">
                            {renderDescription(item.description)}
                          </div>
                        </div>
                      ))}
                    </div>
                  </section>
                )
              )
            case 'projectExperiences':
              return (
                projectExperiences?.length > 0 && (
                  <section key={key} className="mb-10">
                    <MainTitle title="Featured Projects" />
                    <div className="space-y-6">
                      {projectExperiences.map((item) => (
                        <div
                          key={item.id}
                          className="bg-gray-50/50 p-4 rounded-xl border border-gray-100"
                        >
                          <div className="flex justify-between items-baseline mb-2">
                            <h4 className="text-[14px] font-bold text-gray-800">
                              {item.projectName}
                            </h4>
                            <span className="text-[11px] text-gray-400">
                              {formatDate(item.startDate)}
                            </span>
                          </div>
                          {item.role && (
                            <div className="text-[12px] text-gray-500 mb-2 font-medium">
                              {item.role}
                            </div>
                          )}
                          <div className="text-[13px] text-gray-600">
                            {renderDescription(item.description)}
                          </div>
                        </div>
                      ))}
                    </div>
                  </section>
                )
              )
            case 'certificates':
              return (
                certificates && (
                  <section key={key} className="mb-10">
                    <MainTitle title="Certificates & Awards" />
                    <div className="text-[13px] text-gray-600">
                      {renderDescription(certificates)}
                    </div>
                  </section>
                )
              )
            default:
              return null
          }
        })}
      </main>
    </div>
  )
}
```

### 💡 关键代码说明

1. **侧边栏背景计算：** `style={{ backgroundColor: theme.themeColor, filter: 'brightness(0.15)' }}`。这种方式不需要你预先定义深色色号，系统会自动根据用户选的主色调（如亮蓝、翠绿）生成一致性的深色背景，极大地减少了配置成本。
2. **教育经历的条件渲染：** 在 `main` 的循环中增加了 `key === 'educations'` 的过滤，并将其手动放置在 `aside` 中，确保数据逻辑与 UI 结构解耦。
3. **右侧视觉锚点：** `MainTitle` 使用了一个带渐进背景的布局，并在下方增加了一个主色调的小色块（Pill），这能让白色的正文区显得非常有质感，不至于单调。
4. **工作经历的时间轴：** 引入了 `border-l` 和主色调圆点，模拟了“职场进阶”的线性感，非常适合这个模板追求的“设计感”。
